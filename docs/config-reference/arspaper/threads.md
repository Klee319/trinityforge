# arspaper/threads.yml

出荷config `fork-handoff/arspaper/fork/src/main/resources/threads.yml` の本文コメント(先頭ヘッダを除く)をここに移設したもの。
config-editor で保存すると本文コメントは復元されないため([tools/config-editor/lib/yamlio.js](../../tools/config-editor/lib/yamlio.js) 参照)、運用ドキュメントとしてここに退避している。
**設定値そのもの(キー/値)はymlの方を参照。ここはコメント本文のみを収録し、移設前後でキー・値が完全一致することをスクリプトで検証済み。**

## 本文コメント一覧(元のyml内での出現順)

```
  # ============================================================
  # 空スレッド（基本素材）
  # ============================================================
```

```
  # ============================================================
  # 効果付きスレッド - 重複可能（stackable）
  # ============================================================
```

```
  # コスト軽減はプレイヤー全装備の合計が100%を超えないよう内部で制限されます。
  # 1つあたりの軽減率(cost-reduction)やmax値を増やしすぎると、
  # 全スペルのマナ消費が実質1（最低値）になる可能性があるため注意してください。
```

```
  # ============================================================
  # 効果付きスレッド - 重複不可（仕様上1つで十分）
  # ============================================================
```