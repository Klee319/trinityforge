# arspaper/sourcejars.yml

出荷config `fork-handoff/arspaper/fork/src/main/resources/sourcejars.yml` の本文コメント(先頭ヘッダを除く)をここに移設したもの。
config-editor で保存すると本文コメントは復元されないため([tools/config-editor/lib/yamlio.js](../../tools/config-editor/lib/yamlio.js) 参照)、運用ドキュメントとしてここに退避している。
**設定値そのもの(キー/値)はymlの方を参照。ここはコメント本文のみを収録し、移設前後でキー・値が完全一致することをスクリプトで検証済み。**

## 本文コメント一覧(元のyml内での出現順)

```
  # 【capacity 10,000 → 20,000 (2026-08-08)】
  #   TF カタログの儀式191件のソース費用は中央値 15,000 で、10,000 のままだと
  #   191件中99件が「ジャー1個では足りない」= 最初のジャーを作った直後の儀式がまず通らない。
  #   儀式は半径5・y±2 の箱にあるジャーを全部合算して吸うので複数置けば回るが、
  #   「基本ジャーが典型的なレシピ1回分すら入らない」のは入口として重すぎる。
  #   20,000 にすると中央値を1個でまかなえて、なお191件中55件は複数ジャーが要る
  #   (=下の「増やす vs 良いのに替える」パズルは残る)。
  #   ⚠ この値を変えると上位ジャーとの比も変わる (source_jar_ii へは 5.0倍 → 2.5倍)。
```

```
    # バニラ Ars: Archwood Slab + Glass。本ポートでは Sourcestone 系素材に置換した作業台レシピ。
    # (2026-08-23 に docs/config-reference/arspaper/sourcejars.md から復元 ── 設定エディタで
    #  保存された際に yml 側から落ちていて、退避先にしか残っていなかった)
```

```
    # クリエイティブ専用 — クラフトレシピなし
    # (2026-08-23 に docs/config-reference/arspaper/sourcejars.md から復元)
```

```
  # ===========================================================================
  # 上位ジャー階梯 (2026-07-31 追加) — 「1億ソース」目標の貯蔵側
  # ---------------------------------------------------------------------------
  # 【なぜ容量を上げる意味があるのか】
  #   儀式は「儀式コアから半径5・高さ±2 (11x5x11 = 605マス) 内のジャーを全部合算して」ソースを引く。
  #   つまり1回の儀式に注げる上限は「置ける数 x 1個の容量」。床面積は605マスで固定なので、
  #   容量段階が上がると「同じ床で何倍のソースを一度に注げるか」が変わる。
  #   → 「ジャーを増やす」と「良いジャーに替える」の2軸パズルになる (これが building の面白さ)。
  #
  # 【2026-07-31 に fork 側で直したこと】
  #   それまで容量は static 1値で、ジャーは source_jar / creative_source_jar の2種だけが
  #   ハードコードで登録されていた。つまり<ここに何を書いてもブロックとして存在せず、
  #   置くことすらできなかった>。今は jars: に足すだけで
  #     ・ブロック/アイテムとして登録される (ArsPaper#registerCustomSourceJars)
  #     ・容量が個体ごとに引かれる      (SourceJar#maxSource(TileState))
  #     ・儀式が吸う / ソースリンクが注ぐ / パーティクルが出る / /ars give で出せる
  #   のすべてが追従する。
  #
  # 【累積ソースの見立て】(儀式コスト + 素材の作成に要したソースを積み上げた概算)
  #   source_shard 100 → source_jar_ii 900 → source_crystal 3,300 → source_jar_iii 26,100
  #   → source_condenser 86,400 → source_reservoir 671,700 → source_engine 1,691,200
  #   → source_vault 約1,000万 → source_singularity_jar 約2,300万 → infinity_source_core 約1.2億
  #   (2026-08-16 に source_vault を挟んだぶん、最上位までの積み上げが約1,043万 → 約2,300万へ増えた)
  #   触媒側 (source_shard 等) は materials.yml に定義。燃やしたときの点数は sourcelinks.yml。
  #
  # 【段の細分化 (2026-08-23 / W-189)】上の見立ては 5段だった頃のもの。触媒ごとに
  #   「その触媒で作れる段」が1つしか無く、触媒を切り替えた瞬間に次の壁へ飛んでいたので、
  #   欠片/結晶/凝縮物/機関のそれぞれに1段ずつ足して 10段にした:
  #     20,000 → 50,000 → <b>150,000</b> → 250,000 → <b>700,000</b> → 2,000,000
  #     → <b>5,000,000</b> → 10,000,000 → <b>25,000,000</b> → 50,000,000
  #   倍率は 2.5/3/1.67/2.8/2.86/2.5/2/2.5/2 倍で、以前の最大8倍・25倍のような
  #   「1段だけ極端に重い」場所を消してある。
  #   ⚠ 追加した段はどれも前段を core-item に取るので、階梯は一本道のまま。
  #     途中の段を消すときは<b>次段の core-item を必ず付け替える</b>こと。
  #
  # 【バランスの調整レバー】capacity と recipe.source の2つだけ。触媒の「燃やすと何点か」は
  #   sourcelinks.yml 側。段数を増やしたい/減らしたいときは、ここに段を足すか消すだけでよい
  #   (Java 側は id を一切知らない)。
  #
  # ⚠ このコメント塊は 2026-08-08 に復元したもの。設定エディタで sourcejars.yml を保存すると
  #   本文途中のコメントが落ちる (yamlio.serializeConfig の既存仕様)。落ちていたら
  #   git の履歴から戻すこと — ここが階梯の設計意図が書いてある唯一の場所。
  # ===========================================================================
```

```
  # 【source_amphora を挟んだ理由 (2026-08-23 / W-189)】
  #   蒼片の壺(50,000)の次が結晶の大甕(250,000)で、そこで初めてソース結晶が要る。
  #   ソース結晶は儀式1回ぶんの触媒としては重く、「欠片で回している段階の伸びしろ」が
  #   壺1種しか無かった。欠片だけで届く段をもう1つ置いて、結晶へ進む前に
  #   「増やす vs 替える」を一度やらせる。
  #   ⚠ 結晶の大甕の core-item をここへ差し替えているので、この段を消すなら
  #     core-item も custom:source_jar_ii へ戻すこと(戻し忘れると以降が全部作成不能になる)。
```

```
  # 【source_cistern を挟んだ理由 (2026-08-23 / W-189)】結晶で作れる段が大甕1つだけで、
  #   結晶を集め切った瞬間に次は「凝縮物」という別素材へ飛んでいた。結晶の使い道を
  #   もう1段伸ばして、凝縮物の解禁までを緩やかにする。
  #   ⚠ ソース貯水槽の core-item をここへ差し替えている(消すなら custom:source_jar_iii へ戻す)。
```

```
  # 【source_basin を挟んだ理由 (2026-08-23 / W-189)】凝縮物で作れる段も貯水槽1つだけだった。
  #   ⚠ 深淵の櫃の core-item をここへ差し替えている(消すなら custom:source_reservoir へ戻す)。
```

```
  # 【source_vault を挟んだ理由 (2026-08-16)】
  #   ここまでの段は 2.5倍 → 5倍 → 8倍 と伸びていたのに、貯水槽(2,000,000)から
  #   特異点の壺(50,000,000)だけが<b>25倍</b>で、儀式コストも 250,000 → 2,000,000 の1段しか無かった。
  #   つまり「貯水槽をひたすら並べる」だけで最後の壺まで届いてしまい、階梯の最後だけ
  #   建築パズル(増やす vs 替える)が成立していなかった。5倍 x 5倍の2段に割って、
  #   最後の1段を「材料は同じだが桁がひとつ重い」壁にする。
  #   ⚠ 2026-08-23 (W-189) に前後へ段を足したので、この段を消すときに戻す先は
  #     「core-item を custom:source_basin → custom:source_reservoir」ではなく、
  #     <b>次段 source_abyssal_urn の core-item を custom:source_basin へ付け替える</b>こと。
```

```
  # 【source_abyssal_urn を挟んだ理由 (2026-08-23 / W-189)】ソース機関で作れる段が
  #   深淵の櫃と特異点の壺の2つだが、特異点の壺は深淵の櫃の5倍と重く、機関を集める工程が
  #   最後だけ一気に膨らんでいた。機関を使う段をもう1つ置いて負荷を割る。
  #   ⚠ 特異点の壺の core-item をここへ差し替えている(消すなら custom:source_vault へ戻す)。
```
