# TrinityForge - 伐採ギミック(各 skilltree/*.yml ノードの dedicated-effects: フィールドが付与する flag系consumer)チューニング
# 対象effectId: tree-fell (2026-07-25 gather-rework-active-framework §6 Q1: 旧small-tree-fell/
# large-tree-fell を統合。tierはノード側 dedicated-effects[].value で持つ)
# ドロップテーブル(drop-tables)は「葉/原木破壊」トリガー型の追加ドロップ(2026-07-23 stat-gate-overhaul §4)。
# 数値は全て暫定(要調整)。
# 設定リファレンス(本文コメント移設先): docs/config-reference/stats/woodcutting-gimmick.md

tree-fell:
  max-extra-logs: 8
  cooldown-ticks: 200
  # 幹に繋がる葉も一緒に壊すか(2026-07-30)。false にすると原木だけ消えて葉が空中に残る。
  break-leaves: true
  # 1回の伐採で壊す葉の絶対上限(2026-07-31 N2)。tiers 側に leaves-max があればそちらが優先。
  # 0以下にすると旧挙動(leaves-per-log × 実際に伐った原木の本数)へ戻る。
  leaves-max: 512
  # 旧キー(2026-07-30)。leaves-max を0以下にしたときだけ「伐った原木の本数 × この値」として使う。
  # 葉は「伐採のおまけの片付け」なので道具の耐久も採取EXPも取らない(TreeFellingListener 参照)。
  leaves-per-log: 6
  # 1tickあたりに壊す葉の枚数(2026-07-31 N2)。512枚なら約11tick=0.55秒で樹冠が消える
  # (バニラのランダムティック崩壊は平均68秒、simulation-distance の外では止まる)。
  # 0以下なら同tickで全部壊す — チャンク更新とライティング更新が集中するので非推奨。
  leaves-per-tick: 48
  # true なら「バニラなら崩壊する葉」だけを壊す(2026-07-31 N2)。
  # = 設置された葉(persistent、装飾の葉壁)は壊さず、残った原木から距離6以内で支えられた葉も残す。
  # 上限を桁で上げたので、false にすると樹冠が癒着したジャングル/ダークオークで
  # 「1本伐ると林冠が連鎖消滅する」。既定 true を崩さないこと。
  leaves-decay-only: true
  tiers:
    "1":
      max-extra-logs: 8
      leaves-max: 128
    "2":
      max-extra-logs: 16
      leaves-max: 256
    "3":
      max-extra-logs: 32
      leaves-max: 512
    "4":
      max-extra-logs: 64
      leaves-max: 1024
drop-tables:
  categories:
    apple:
      display-name: リンゴ
      trigger-chance-percent: 5
      entries:
        - item: APPLE
          weight: 1
          amount: 1
    golden_apple:
      display-name: 金のリンゴ
      trigger-chance-percent: 1
      entries:
        - item: GOLDEN_APPLE
          weight: 1
          amount: 1
    crystal_apple:
      display-name: クリスタルリンゴ
      trigger-chance-percent: 0.5
      entries:
        - item: tf_crystal_apple
          weight: 1
          amount: 1
