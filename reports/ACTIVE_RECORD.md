# TrinityForge — 現役記録（残タスク / 既知の問題 / 現在の状態）

**この文書が唯一の現役記録です。** 残タスク・バグ・現在の状態はここだけを見て、ここに追記する。

- **過去の作業記録は `reports/ACTIVE_RECORD_ARCHIVE.md`**（2026-08-04 に切り出した。解決根拠と失敗の型を辿る用）。
- `docs/` 直下の `*_SPEC.md` は仕様、`docs/config-reference/` は config キーの一次情報、
  `docs/agent-context/` は「触る前に知らないと黙って壊す知識」。`docs/archive/` は**失効した設計文書**。
- **新しい日付入りレポートを増やさない。** 2026-07-27 に 47 本まで膨れて全部 stale になり、
  2026-08-04 に削除した（git 履歴にはある）。

## 運用ルール

- 項目を閉じるときは行を消さず、`~~取り消し線~~ → 解決日と根拠` にする。
  §2〜§4 が長くなったら、**閉じた行をアーカイブの §A へ移す**（消さない）。
- 作業記録は §7 に**上へ**追記する。長くなったら古い順にアーカイブの §B 先頭へ移す。
- **着手前に必ず実コードで裏を取る。** 棚卸しをすると毎回「記録が腐っていた」が出る
  （2026-07-27 に 15 件、2026-08-01 に 5 件、2026-08-03 にさらに数件）。
- **「機構は実装済みで config に値が入っていないだけ」は残タスクにしない**（→ §5）。
- **直したと書く前に「修正前に戻すと落ちるテスト」を示す。** 証明のない修正は W-23 に積まれる（現在 12 件）。
- **2026-08-24 全面棚卸し（ユーザー指示）**: 完了・棄却済みの行と節は取り消し線のまま残さず**アーカイブへ移した**。以後も閉じた項目は溜めずにアーカイブ §A/§B へ移してよい（解決根拠を必ず添える）。

---

## 1. 現在の状態（2026-08-24 棚卸し時点）

| 対象 | 状態 |
|---|---|
| `dev` HEAD | `8d7d375`（EXP解呪の良薬 W-216 ＋ 魔法追加草案 W-217 の切り出しまで）。この棚卸しで閉じた行・節をアーカイブへ移した（下の運用ルール参照） |
| **テスト基準値（2026-08-24 実測）** | TF **4656 / 32 failed / 2 skipped**（失敗 32 は他セッションの未コミット yml 由来。良薬関連の失敗は 0）。**skip 2 が正常値**は不変。**共有ワークツリーで測った数字は帰属判定に使えない** ── 帰属を言うときはコミット単位でクリーン worktree を作って測る（2026-08-14 の教訓。詳細はアーカイブ §B） |
| 他セッション | 未コミット yml 多数（`items/catalog.yml`・`combat/mob-types.yml`・skilltree ほか）。**未コミット yml はユーザーのエディタ編集＝load-bearing**（HEAD へ戻すと挙動が変わる）。触る前に `git status` |
| ビルド済み jar | TF `TrinityForge/build/libs/TrinityForge-0.1.0-SNAPSHOT-all.jar`（16,632,873 bytes / 08-24 20:43）／ArsPaper `fork-handoff/arspaper/fork/build/libs/ArsPaper-1.0.0.jar`（1,211,100 bytes / 08-24 12:54）。EliteMobs / GeyserExtra / PixelRank は §3.5 参照 |
| 実サーバ | 稼働中前提。**稼働中の jar 差し替えは必ず `NoClassDefFoundError`**。配備はサーバ停止後に §3.5 の手順で（実行はユーザー） |

---

## 2. 残タスク — ユーザー判断待ち（「何を判断するか」を各行に明記）

**2026-08-25 に旧 D-2〜D-18 と実装側 14 件の判断がすべて出た。** 決定内容は §3 の各行に埋め込んである。
ここに残っているのは未回答の 1 件、ユーザー側で調べると宣言された 1 件、
そして **2026-08-25 の実装着手中に実物から出てきた 3 件**（D-20〜D-22）。

| # | 判断すること | 背景 |
|---|---|---|
| **D-1** (W-217) | **魔法追加 11 件の草案を承認するか。** 特に5点: ①剣投擲(B)の差別化方式 ②「再生魔法の ban」の実体が未特定のまま、どう解禁するか（グリフ自体が存在しない） ③エリトラを軽装扱いへ変える範囲 ④「軽装90 かつ 重装90」を書ける複数形キー `use-level-requirements:` の新設可否 ⑤着手順 | 草案本体 = `docs/design/2026-08-24-magic-additions-draft.md`。承認まで実装しない |
| **D-19** | **スレッドに付ける使用可能レベル制限の「対象と値」**（2026-08-25 ユーザー「一旦こちらで確認する」） | 方針は確定済み（→ §3 W-222）: **飛行スレッドは Lv100**／ダンジョンドロップの強いスレッド（品質ではなく基礎ステの相対差が大きいもの）を優先／トレジャーチェスト産も戦闘系なら強さに応じて設定。**どのスレッドに何レベルを割り当てるかの一覧はユーザーが確認して確定する。** 機構（`use-level-requirement` をスレッド装着の門にする）は先に実装してよい |
| **D-21** | **ダンジョンの印 28 種の「別の出口」をどんな形にするか** | 【2026-08-25 ユーザー「保留」】案（①難易度帯ごとに束ねて既存の券・素材へ交換 ②同じダンジョンの鍵へ戻して周回可能にする ③ダンジョン固有のコスメ・称号へ替える／消費の重さは「同じ帯を数個」「1個ずつ」「種類を問わず枚数だけ」）を提示したが**保留**。⚠ **調べて分かった前提**: 儀式の台座は既に `custom:dungeon_seal_*` を素材として受け付ける（世界を繋ぐ装備 13 件が `dungeon_seal_binder` を要求している）ので、**どの案を採っても新しい仕組みは要らない**。ただしその 13 件は全て `draft: true`（未出荷）なので、**出荷状態では 28 種すべてが消費先ゼロ**という事実は変わらない。実装は → §3 W-238 |
| **D-23** | **エンチャント運の見せ方**（2026-08-29） | 内部は「運 × 2%」（運50で格上げ/追加/上限突破が各100%）。**運 10 未満はバニラより弱くする**（W-301）。Wiki には書いた。数値そのものを%表示にするか、係数を 1%=1運 に揃えるかは未決 |

### 2-B. ユーザーからの情報・実機確認待ち

| # | 待っている情報 | 背景 |
|---|---|---|
| **I-4** (W-108残) | 「奇妙なポーション」単体の症状が **Java 版クライアントでも出るか** ── 2026-08-25 ユーザー回答「はい（自信が無いのでこちらでも確認して）」 | 【2026-08-25 こちら側の追検証は完了＝白】出荷ソースを全走査した結果、**TF は「奇妙なポーション」に一切干渉していない**。根拠3点: ①TF 本体の主ソースに `AWKWARD` という語が**1つも無い**（出てくるのはテストと ArsPaper 側の GUI 表示ラベルだけ）②解放式ポーションの門（`brew-unlocks`）が見ているベースは `THICK` と `MUNDANE` だけで **`WATER` の枠が1つも無い**（この門は「ベースの種類＋素材」の組でしか弾かないので、水入り瓶＋ネザーウォートには構造的に当たらない）③錬金のEXP表は `AWKWARD` に 150 を配っている＝作れる前提の設定。同日追加したカスタム効果ポーションの醸造禁止も素の水入り瓶は通す。→ **残るのはユーザーの実機確認だけ**。`ShippedVanillaAwkwardBrewIsUntouchedTest` が②を、`PotionCustomEffectBrewGuardTest` が禁止側を固定している |
| **W-240** | **設定エディタで編集中の武器ステータスが、DPS の帯と杖の攻撃速度の2つの取り決めを外れている** | 【2026-08-25 発見・ユーザー確認待ち】`stats/item-stats.yml` の**未コミット分**（＝ユーザーが設定エディタで編集した実データ。**エージェントは戻さない**）が、出荷データを読む2つの検査を赤にしている。⚠ **配備は「HEAD にワーキングツリーの yml を重ねる」方式なので、この未コミット分はそのまま出荷される。** ①**戦鎚7本の実効DPSが剣の96〜99%**（取り決めの帯は 82〜94%）── 戦鎚は「一撃の重さで差を付ける代わりに手数で劣る」設計なので、剣とほぼ同じDPSだと重武器を選ぶ理由が消える。②**杖10本の近接攻撃速度が 1.4**（HEAD では 0.1）── 検査は「遠隔武器の近接攻撃速度は最低値で揃える」を固定しており、触媒2種は今も 0.1。杖だけ 1.4 だと**杖で殴るのが成立する**。⚠ どちらも**意図した再調整なら検査の側を直す**、意図しない変更なら値を戻す、の二択。**どちらかはユーザーにしか分からない**ので勝手に触らない。⚠ 同じ未コミット分には `*_HOE#NN` → `*_SWORD#NN` の素材付け替え11件も入っている（CMD はそのまま） |

---

## 3. 残タスク — 手を動かせば終わるもの

**2026-08-25 のユーザー判断でここへ降りてきた行には `【決定】` を付けてある。** 決定内容を勝手に読み替えない。

### 3-A. 2026-08-25 の判断で確定した実装

| # | 内容 | 決定と前提 |
|---|---|---|
| **W-63** | **モブスキルに予兆が無く、発動頻度も高すぎて避けようがない** | 【決定】**予兆は「詠唱時間＋パーティクル＋音」のセット**／**反応猶予 1.5〜2 秒**／**詠唱中に攻撃を当てると発動を中断（詠唱妨害あり）**／**技ごとに設定可能**（全スキル一律にしない）。既知の罠: パーティクル個数 0 は「消える」ではない／`Particle.FLASH` は Color 必須。頻度側は対応済み |
| **W-222** | **スレッドの抽選幅を見直し、あわせて使用可能レベル制限を新設する** | 【決定】見直す。**前提は「全身＋手持ちで最大 25 枠」**（合計 100% 超の死に値が出ない幅にする）。**新設: スレッドに使用可能レベル制限を持たせ、それ未満の装備には挿せない**（飛行スレッドは Lv100）。対象と値の一覧は D-19 でユーザーが確定。機構は先に作ってよい |
| **W-227** | **ArsPaper `items.yml` 残り 2 件（`trident` / エンチャントされた金リンゴ）を移設する** | 【決定】**「その他のギミック」のバニラレシピ追加へ移す**。⚠ **移設先の権限（解放ゲート）が Ars と TF で衝突しないよう注意する**（`resolveGateId` は名前空間を問わず path をゲート id にするので、同名 id は両方に掛かる） |
| **W-228** | **ArsPaper フォーク `feat/trinityforge-fork` を push する** | 【決定】push する。⚠ **`Klee319/ArsPaper` は public** で、このブランチには tracked の `libs/TrinityForge.jar` が入っている。**push 前に jar の扱い（履歴からの除外か追跡解除）を決めてから**行う |
| **W-236** | **ドリリングのゴーストブロックを直す** | 実機確認で **F3+A（チャンク再描画）で直る＝ゴーストブロック確定**。`drilling.yml` の Lv4 は 1 回の破壊で周囲 20 マス超を `breakNaturally` するため、クライアントに残ったブロックを叩いても `BlockBreakEvent` すら飛ばない。**サーバが消したブロックをクライアントへ確実に通知する**（範囲破壊後の再送）|
| **W-237** | **統合版のカスタム品フォールバック不足と防具アタッチャブルの再計測** | 08-23〜24 の GeyserAddon 改修（焼きアイコン 959 枚・台帳修復・展開競合）でどこまで解消したかを**まず実測してから**残件を詰める（旧 W-47。104 件＋7 種という数字は改修前のもの） |
| **W-238** | **ダンジョンの印 28 種に別の消費先を作る** | 【2026-08-25 保留】出口の形が D-21 で保留になったので**着手しない**。⚠ 再開時に前提として使える調査結果: ①印と鍵は 28 対 28 で名前が 1 対 1（`dungeon_seal_<名>` ⇔ `key_<名>`）②難易度 1〜10 は `combat/mob-overrides.yml` のコメントが一次情報で、実績と図鑑の並びは 2026-08-25 にその昇順へ揃えた③儀式の台座は既に印を素材として受ける④ガチャ券は `gacha.yml` 側の定義でカタログには無いので、カタログの `recipe:` で直接作らせることはできない⑤印を増減させたら `progression/collection.yml` の一覧と必ず揃える（`ShippedDungeonKeyReachabilityTest` が食い違いを落とす） |

### 3-B. 判断で「まず確認」になったもの（結果しだいで閉じる）

| # | 確認すること | 背景 |
|---|---|---|
| **C-2** | **inv テクスチャ設定済みの防具は装備モデルも自動生成されているか** | ユーザー「inv テクスチャ設定済みのものは自動で装備モデルも生成されるはず」。`build_armor_layers_from_items.py` は inv テクスチャの配色から 64x32 レイヤーを機械生成する設計なので、**実際に何セット配線されているかを数える**。⚠ **inv テクスチャ未設定の防具は【決定】現状維持で保留**（絵が来るまで着手しない） |
| **C-3** | **階梯ソースリンク 20 件の CMD 割り当ては統合版まで通るか** | 【決定】**割り当ててよい**（色調補正で機械的に区別できる）。**ただし GeyserExtra（改造可）を加味しても統合版への反映が厳しいならやらない**。→ 統合版側の実現性を先に確認してから採番する |
| **C-7** | **準備中(draft) 79 件が統合版へ本当に漏れているか** | ユーザー「Java は更新しているから統合版も GeyserExtra でリパックされるのでは」。**現在の配信パックと現在のパイプラインで実際に起きているかを実測**してから対応を決める |

### 3-C. 判断不要でそのまま進めるもの

| # | 内容 | 前提 |
|---|---|---|
| ~~**W-312**~~ | **地面に落ちたアイテムが 99 個超になり、保存できずに消える**（2026-09-04 実機報告 → 同日修正） | 実機ログ: `ItemEntity['Rotten Flesh']: Failed to encode value '259 minecraft:rotten_flesh' ... Value must be within range [1;99]`。**真因**: `MobDropRoller#extraCount` はドロップ増加ステの整数部をそのまま加算し、上限が `MAX_EXTRA_COUNT = 256`。`cappedCount` の上限は `maxStackSize × 8 = 512` なので **1 スタック 257 個がそのまま `dropItemNaturally` される**。1.21 のアイテム個数コーデックは **上限 99** なので、その ItemEntity はシリアライズに失敗し、**チャンク保存時に捨てられる（＝プレイヤーから見ると拾う前に消える）**。物証: Resource_Server 18:27:03 に同一座標で 6 スタックが全て `1+256=257`（腐肉だけ `3+256=259`）＝**flat +256 が全ドロップに乗った**。警告は Main_Server の最新ログだけで 915 件。**修正方針**: 個数を `min(99, maxStackSize)` ごとに**分割して複数エンティティで落とす**（1 スタックへ詰めない）。あわせて `MAX_EXTRA_COUNT` とドロップ増加ステの実効上限を見直す（ステ側が 256 に張り付いている） **【2026-09-04 解決】** 地面へ落とす全経路を新設 `items/ItemStackDrops`（`split`/`dropSplit`/`giveOrDropSplit`）へ通し、`min(99, 最大スタック数)` ごとに**分割して複数エンティティで落とす**ようにした。`addItem` へ渡す前に分割するのが要点（満杯時に巨大スタックがそのまま leftover で返るため）。対象は モブドロップ3経路・採掘/伐採/採掘ギミック・釣り・ガチャ・図鑑/実績報酬。`cappedCount` の計算は据え置き（総個数の設計は不変）。回帰テスト `MobOverrideDropListenerLargeCountRegressionTest` が「ドロップ増加300で257個 → 全て99以下・3本以上に分割」を固定。 |
| ~~**W-313**~~ | **レベル到達の全体放送が連投される**（2026-09-04 実機報告 → 同日修正） | 実機ログ `Main_Server/logs/2026-09-02-2.log.gz` 19:48〜19:50 で `Ars魔法 Lv80→90→100` が **5 周**繰り返す。**真因はプレステージ**: `NativePerkService#prestigeUnderLock` はスキルを `SkillProgress(0, 0.0, 0.0, tier, max)` へ戻す（レベルも累計EXPも 0）。放送側 `SkillLevelBroadcastListener` は**プレステージ段を見ておらず**、登り直しのたびに節目を全体へ流す。DB 実測（`player_progression.db`）で当該プレイヤーの Ars魔法・弓術・エンチャント等が **`prestige = 5`（上限）** と一致＝**5 回のプレステージ＝5 周の放送**。**二重放送のバグではない**（畳み込みは (プレイヤー,スキル) ごとに 1 tick で最高値へ正しく畳んでいる）。**併発している本質的な問題**: 累計EXPが `ENCHANTING = 1.5e10` / `ARS_SMITHING = 9.3e12` と設計を桁で超えており、**プレステージ直後に数秒で Lv100 へ戻る**。放送の抑止だけ入れても EXP 供給側を直さないと再発する。**物証（DB のプレステージ perk 解放時刻と放送が1対1で一致）**: `arsmagic_perk_ng1..ng5` = 19:48:40 / 19:48:58 / 19:49:16 / 19:49:40 / 19:49:59 に対し、放送の 80→90→100 の各周は 19:48:43 / 19:49:04 / 19:49:22 / 19:49:45 / 19:50:01 開始。**プレステージの約3秒後に必ず登り直しが始まる**。重装備（`heavyarmor_perk_ng1..5` 09-03 17:55:11〜17:55:55）・エンチャント（`enchanting_perk_ng1..5` 09-02 20:29:40〜20:31:44）も同様に一致。**これで「巻き戻し書き込み」説・「二重発火」説は棄却**。**同時に判明した重大事**: プレステージ直後の Lv0 から **約11秒で Lv100 へ戻っている**（0→80→90→100）。EXP 供給が設計から桁で外れている証拠。**修正方針**: ①放送に「同じプレイヤー・スキル・節目はプレステージ段ごとに 1 回」の抑止を入れる（永続化が要る）②EXP 供給の異常値を別途調査（W-312 のドロップ増加ステ張り付きと同根の疑い） **【2026-09-04 解決】** ①プレステージ段が1以上のスキルは**上限レベル到達だけ**放送（途中の節目は出さない）②`min-interval-seconds`（既定30・0〜3600）で同一プレイヤーの放送を流量制限 ③段つきの文言 `message-prestige`（`%prestige%`）。段の取得元が未配線・例外なら従来どおり全部放送（安全側）。退出でエントリを掃除。⚠ **経験値供給の異常は別問題**（W-315）。 |
| ~~**W-314**~~ | **経験値がまれに黙って消える（TOCTOU）**（2026-09-04 調査中に発見 → 同日修正） | `NativeExperienceDispatcher#grant` は `pending.get()` で現在の Map を取ってから `merge` する。`drain()` は `pending.getAndSet(new ConcurrentHashMap<>())` で参照を差し替えるため、**その隙に走った `grant` は「もう捨てられた古い Map」へ書き込み、その EXP は例外もログも無く失われる**。高頻度で EXP が入るスキルほど当たりやすい。あわせて指摘: ①`ProgressionCurveReconciler` は**ロック無しで絶対値を書く**ので `/trinityforge reload` と EXP 付与が重なるとロストアップデートの余地がある ②進行データのキャッシュ TTL に**無関係な `combat-level.yml` の `cache.ttl-seconds` を流用**している（`TrinityForge.java` 付近）。書き込み時に invalidate しているので現状は実害なしだが、依存として不健全 **【2026-09-04 解決】** ①`NativeExperienceDispatcher` の `pending` を単一 `ConcurrentHashMap` にし、drain を**キー単位の CAS `remove(key, snapshot)`** へ変更（割り込んだ付与は次回へ繰り越すので構造的に落ちない）。決定的にレースを再現するフックと 6スレッド×2000回の並走テストを追加 ②`ProgressionCurveReconciler#recalculatePlayer` を**共有 `PlayerLockRegistry`** でプレイヤー単位に囲んだ ③進行キャッシュの TTL を `stats/skill-exp.yml` の `cache.ttl-seconds`（既定3秒＝従来と同値）へ分離し、戦闘レベル表示の設定への相乗りをやめた。 |
| **W-315** | **経験値の供給が設計から桁で外れている**（2026-09-04 起票。**一部のみ対処済み・調査継続**） | 実測: 累計EXPが `ARS_SMITHING = 9.3兆` / `MINING = 322億` / `ENCHANTING = 150億`（Lv100 の必要累計は約720万）。**上限到達後も累計は無限に積まれる設計なので累計値そのものは異常の証拠にならない**。問題は**速度**で、プレステージ直後の Lv0 から **約11秒で Lv100 へ戻る**（管理コマンドでの付与は無い＝実プレイで入っている）。**判明した原因（対処済み）**: 儀式のソース由来EXP `ars-smithing.exp-per-source` が **0.2**（同じ yml のコメントが「0.001 程度から様子を見ること」と書いている値の200倍）で、しかも `ArsProgressionBridge#sourceExp` に上限が無かった。最上位の儀式（ソース4,500万）1回で 900万EXP＝Lv100 相当が単発で入る。→ **天井 `ars-smithing.max-source-exp-per-craft`（既定100000）を新設し、下位の儀式は不変・上位だけ頭打ちにした**（`ArsSourceExpCapTest`）。**未解決**: これは鍛冶系しか説明しない。**Ars魔法・エンチャント・弓術が11秒で再到達する経路は未特定**。呼び出し量を握っているのは ArsPaper フォーク側で、**フォークのソースは `.gitignore` 除外のためこの worktree に存在せず未確認**。**【2026-09-04 決定】出荷値 `exp-per-source: 0.2` は据え置き**（ユーザー「問題なし」）。天井 `max-source-exp-per-craft: 100000` だけで抑える。下位〜中位の儀式の伸びは今までどおりで、最上位だけが頭打ちになる。**再燃したらまず係数を下げる**（0.01 → 0.001 の順で様子を見る）。**残る調査**: Ars魔法・エンチャント・弓術が11秒でLv100へ再到達する経路は未特定。呼び出し量は ArsPaper フォーク側にあり、**フォークのソースはこの worktree に存在しない**ので、追うにはフォークの取得が要る |
| **W-310** | **ダンジョン改修: 闇の大聖堂1本を予告付きで出す**（2026-09-04 起票。機構は `2ad3e85` で揃った） | 設計正本の手順どおり ①大聖堂の全モブの現行 `abilities:` 割当表を書く（残す/差し替え/削除）②予告を紫(直線)と金(床印)の2色に寄せ、テンプレートへ `cast-seconds`/`lethal` を書く（雑魚には配らない）③机上検査を全技に通す（式は 2026-09-04 午後に `半径 ≤ 状態別速度×max(0,詠唱秒−D95)−余白` へ置換。`TelegraphFeasibility#walkable` が実装。D95=0.35s は仮置きで実測後に差し替え） ④受入試験。⚠ 数値は円化の実測を取るまで据え置き（円化と数値調整の効果を混ぜない） |
| **W-311** | **予告機構の実機確認（JUnit では測れない項目）** | Java＋統合版で: 輪・2レールが暗所/水中/一人称で読めるか／アクションバーの `▮▯` が Bedrock のフォントで潰れないか（潰れるなら `telegraph-bar-style: ascii` へ）／**D95（詠唱開始→最初の権威的移動tick到着）の実測**（100回撃って95%点。`TelegraphFeasibility.D95_SECONDS` 0.35 を差し替える）／詠唱中の向き書き戻し（毎tick `setRotation`）がガクつくか（不快なら外す。幾何はスナップショットなので判定は変わらない）／EXP を actionbar 表示にしている人で予告中にEXPが消えることの体感。Codex 指摘 #3/#6/#7（`docs/design/2026-09-04-telegraph-ux-review-codex.md`）の残課題。検証サーバ（debugMCP）の TF アダプタは未着手 |
| **W-68** | **ガチャ券から出た空のスレッドがスタックされない** | 【決定】原因側を直したうえで**既に配られた個体も救済する**。**2026-08-25 調査で真因確定＝原因側は既に塞がっている。残るのは救済だけ**: 空スレッドは `catalog.yml` に `external-source: arspaper` が付いたので今は Ars 側 `ThreadItem` で作られ PDC を持たない。**宣言が無かった頃は TF カタログ経由（`ItemAssembler#assemble`）で作られ、`roll_seed`／`quality`／`data_version`／`table_generation`／`catalog_id` が無条件で焼かれていた**（`roll_seed` は抽選ごとに新規発番なので旧個体同士すらスタックしない）。→ 旧個体は `trinityforge:catalog_id == thread_empty` で検出し、上の5キーを剥がして `ThreadItem.fullLore` で lore を組み直せば現行個体と同一視できる |
| ~~**W-57**~~ | ~~武器のスレッド欄のステが `/tf status` に反映されない~~ → 2026-08-29 `SocketedThreadStats` が装備 PDC の `thread_slots` をライブ合算し、addon に無いキーだけ穴埋め。`ArsArmorStatRefreshBridge` は表示直前と reload で既存。`SocketedThreadStatsTest` | フォークは実効 thread-slots 0 だと挿さっていても空書き。スレッドは socketed-only で item マップに乗らない |
| **W-132** | **圧縮素材のホッパー搬入が素通りし、精錬可能な圧縮食材が 9 個分 1 個に焼き潰される** | 【決定】**バグが出ない前提で「圧縮のまま焼ける」を広げてよい**。⚠ **精錬時間は素材の圧縮段数ぶん増やす**（9 倍圧縮なら 9 倍）。**ステータスボーナス（精錬速度）も考慮に入れる**。塞ぐのではなく成立させる方向 |
| **W-22** | **`em_id_enchantment_challenge_1〜9` の 9 ボスが特殊攻撃ゼロ** | 【決定】**技の内容はお任せ**。さらに**技の種類を増やしてよい**（回避・ガードなど、戦闘を複雑にしてマンネリを防ぐ方向）。W-63 の予兆機構と合わせて設計する |
| **W-23** | **「直したが証明されていない」12 件に回帰テストを付ける** | 一覧はアーカイブ「2026-08-03 全面監査」の節。ソース文字列走査型テストは再発を素通りさせるので挙動で固定する |
| **W-217** | **魔法追加 11 件の実装** | D-1 の承認後。草案 = `docs/design/2026-08-24-magic-additions-draft.md` |
| ~~**W-241**~~ | ~~防具EXPの「装備の硬さ」係数が実質死んでいる~~ | ✅ **原因確定・設定エディタのラベルを訂正（2026-08-25）**。式は `被弾EXP ×(1 + 防御率の合計 × exp_multiplier_point)`。2026-08-15 に入力を**防具値（点数・最良4部位で約20）から防御率（[0,1]・同 0.30）へ替えたときコード既定だけ 0.05→3.33 へ引き上げ、出荷 yml の `exp_multiplier_point` は 0.05 のまま**だった（yml があればコード既定は使われない）。係数が `1+20×0.05=2.0` から `1+0.30×0.05=1.015` へ落ちている＝**硬さボーナスがほぼ消えている**。⚠ **数値はユーザーが設定エディタで調整中（未コミットで 0.07）なのでエージェントは触っていない。** 最良装備で従来どおり約2倍にしたいなら **3.3 前後**。エディタのラベルは「防具値1点あたりEXP倍率」→「装備の硬さEXP係数」に直し、桁が 1/67 になった旨を説明文へ入れた |
| ~~**W-242**~~ | ~~パーティクルシードの発生位置がプレイヤーの足元固定~~ | ✅ **実装（2026-08-25）**。壊したブロックの中心／殴った相手の胴／矢の着弾点に出るようにした。`origin: player` を書いたシードだけ従来の位置に固定できる。**飛び道具は放った瞬間の武器の刻印を矢の PDC へ移す**（飛んでいる間に持ち替えても最初のシードが出る。弓・クロスボウ・トライデントを `ProjectileLaunchEvent` 1箇所で拾う）。⚠ **魔法（Ars の触媒）は TF 側にイベントが無いので未対応** ── やるならフォークから呼ぶ経路が必要 |
| ~~**W-243**~~ | ~~パーティクルシードに発生パラメータが無い~~ | ✅ **実装（2026-08-25）**。`particles:` と `particle-seeds:` で**同じ語彙・同じ既定値・同じ上限**の発生パラメータ（`shape` / `count` / `radius` / `speed` / `height` / `turns` / `arc-degrees` / `y-offset`）を共有させた |
| ~~**W-244**~~ | ~~パーティクルの形状が2種だけ・速度が常に0~~ | ✅ **実装（2026-08-25）**。形状を **8種**（雲 / 輪 / 球 / 全方向射出 / 螺旋 / 柱 / 弧 / 1点）へ。**形状ごとに効くパラメータが違い、書かなかったキーはその形状の既定値で埋まる**。設定エディタも**形状を変えると入力欄そのものが入れ替わる**。⚠ 真因は「`spawnParticle` の `extra`（速さ）が全形状で 0 のハードコードだった」ことだけではなく、**Bukkit は `count == 0` のときだけ offset を『飛ばす向き』として読む**ので、速さだけ渡しても向きが揃わず拡散にならない点 |
| ~~**W-245**~~ | ~~「右クリックが通ってもバニラの動作が起きない」穴の網羅監査~~ | ✅ **監査完了・1件修正（2026-08-25）**。`PlayerInteractEvent` を見る 17 クラスを判定の型（①報酬・消費か自前完結の操作か ②根拠はクリックか結果か ③結果を見るイベントが無いなら次tickに状態を読み直す）で当てた。**該当は農業EXP 1件**: バニラは**スニーク中で手が空でないならブロック操作を丸ごと飛ばす**ので、熟したベリー／満タンの巣を**スニーク連打すると実が減らないままEXPが入り続けた**（盾ではなくスニークが引き金。W-210 と同じ形）。→ ベリー／ツタは `PlayerHarvestBlockEvent`、蜜は次tickの蜜量確認へ。他16クラスは自前完結の操作かガード（拒否方向）で、`getHand()` と `useItemInHand()==DENY` も入っていた。恒久知識は `docs/agent-context/common-traps.md` の該当節へ追記 |
| ~~**W-246**~~ | ~~設定エディタが保存を拒否する（「circle / aura のいずれかである必要があります」）~~ | ✅ **原因確定（2026-08-25）**。**yml もリポジトリも正しく、走っていたエディタのプロセスが古かった**だけ。Node は `require` したモジュールをキャッシュするので、`lib/schema.js` に形状を足しても**起動しっぱなしのプロセスは古い語彙で弾き続ける**（実測: プロセスの起動 08-21 21:30 / 語彙の追加 08-25 14:30）。つまり**自分が読み込んだ yml の値を自分が保存拒否する**状態。⚠ 対処は**エディタの再起動**（`tools/config-editor/start-config-editor.bat`）＋`public/js` も変えたので**開いているページのリロード**。再発検知として「形状の語彙が Java の enum / `lib/schema.js` / 画面定義の3箇所でそろっている」テストを追加（ずれたら editor のテストが落ちる）。恒久知識は `docs/agent-context/config-editor.md` |
| ~~**W-247**~~ | ~~パーティクルの位置同期が遅く、量が多すぎる~~ | ✅ **実装（2026-08-25）**。**量**: 点ごとに座標が違う形状は点の数だけ `spawnParticle` を呼び、しかも per-viewer 送信なので**実際の量は「点の数 × 近くの人数」**。上限を 400→**64** に下げ、形状ごとの既定と出荷値も一段下げた（球 24→12・螺旋 24→12・射出 20→12、間隔も伸ばした）。**位置**: サーバが知っている位置は 1tick 前のクライアント位置で、**自分の本体はクライアントが予測して即座に描く**ので必ず後ろへズレる → 直前1tickの移動量ぶん先読みした位置へ出す。⚠ **先読みは本人の視点にだけ掛ける**（他人から見た本体は逆に補間で遅れて描かれるので、同じ先読みを掛けると体より前に出る） |
| ~~**W-248**~~ | ~~称号（頭上表示）の位置同期がネームタグより遅い~~ → 2026-08-25 1tick先読み。**2026-08-29 W-299 で称号の先読みは外した**（本人非表示のあと、他人のネームタグより前へ出る） | 目標地点が過去だった。W-263 で前提が変わった |
| ~~**W-249**~~ | ~~足元から後ろへ流れる軌跡の形状が欲しい~~ | ✅ **実装（2026-08-25）**。形状 `trail` を追加（全9種）。`radius`＝後ろへ伸ばす長さ／`speed`＝流れる速さ。⚠ **「後ろ」は向いている方向ではなく進んでいる向きの逆**（横歩き・後ろ歩きでも軌跡は体の後ろへ出る。止まっているときだけ向いている方向の逆）。**この形状だけ既定の高さが足元寄り**（他と同じ胸の高さ 1.0 だと軌跡が空中に浮く）。出荷の実例は `particle_sparkle`（END_ROD） |
| ~~**W-250**~~ | ~~付呪と総合でノードのつながり方が読めない（W-218 で直っていなかった）~~ | ✅ **原因確定・実装（2026-08-25）**。**コネクタの描画は正しく、崩れていたのはノードの座標**。**主軸ノードは最初に全部トランクへ置かれる**ので、根からの深さ順に空きレーンを配ると**どの主軸の扇も、下から伸びてきたチェーンの続きより先にレーンを取る**。押し出された続きは 2〜4 列も横へずれ（付呪 5 箇所・総合 2 箇所。例: 総合 `A-1(4,6)`→`A-2(8,4)`）、コネクタが斜めに長く走る。**ノード行の間に通路の行は1本しか無い**ので、その斜めの線と扇の線が同じ行の**隣り合うセル**に並び、セルの境目が無い GUI では**1本の長い横線に見える**（付呪の最上段は 8 セル連続でこれ）。→ ①**行を下から1つずつ閉じ、同じ行ではチェーンの続きを主軸の扇より先に置く**（続きに内側・扇に外側のレーンを渡す）②**「1段上へ逃げる」コストをレーン4本ぶんに上げた**（以前は横1レーンと同じ重さだったので、行はレベル帯そのものなのに帯を飛ばして置かれていた ── 付呪の `B-1-2` が lv60 なのに lv80 の行に居たのがこれ）。⚠ **W-218 が効かなかった理由**: あれは「空きセルをどう探すか」の修正で、**レーンを配る順序**には触れていない。順序が先に決まるので探索側では直せない。出荷16ツリー実測で**列ずれ 24→0 / 別々の線が1本に見える箇所 18→0 / 帯ずれ 1→0**、接触・迂回・主軸侵入は全ツリー悪化なし。⚠ **座標は yml に無い**（`SkillTreeLayout` が算出）ので**jar の再ビルド・再配備だけで直る。config 配備は不要** |

**閉じたもの（2026-08-25 の判断）**: レベル差ドロップ足きり（`item-threshold: 20`）は**現状維持で確定**／
旧 W-214・W-215（パーティクルシード）は別セッションが実装済み（§7）／
旧 W-49（防具 36 セット）は **inv テクスチャ未設定分を保留**とし C-2 の結果しだいで縮小。

**「力III」の醸造は廃止で確定（2026-08-25 ユーザー決定・旧 D-22）**:
`STRENGTH` の醸造グループは `progression/crafting-features.yml` の `brew-unlocks:` から丸ごと消えており、
旧記録の「`hoglin_tusk` 欠落で作れない」はもう成立しない（`hoglin_tusk` は `hoglin_fang` へ改名済みで参照ゼロ）。
**復活させない。** `ShippedBrewUnlocksPairDriftTest` は全緑なので壊れてもいない。同じ疑問が来たらこの行を示す。

**2026-08-25 に実物を見て「もう直っていた」と分かって閉じたもの**:
旧 W-15（WEAPONSMITH の追加取引が消える）── `VillagerTradeListener#resolveStack` は 2026-08-02/03 に
**TF カタログで解決できなければ ArsPaper のレジストリへフォールバック**するよう直っており（`amount:` の
適用漏れも同時に修正済み）、`ShippedVillagerTradeItemResolvabilityTest` が「`tf_scrap` / `core_jewelry` /
`iron_ingot_scrap` は Ars 供給」を明示的に固定していて全緑。⚠ **この3IDは TF の `items/catalog.yml` には
存在しない**ので、grep で見つからないことを「壊れている」と読み違えないこと。
旧 W-14（`iron_axe_tool` のレシピが登録失敗）── リポジトリ側の `items/catalog.yml` に `iron_axe_tool` は
もう存在せず、`everyReferencedMaterialListIsDefined` も緑。**残っているのは配備先の yml だけ**なので §3.5 へ一本化。

---

### 3-D. 2026-08-25 夜の依頼（8件・着手前の台帳）

**この8件は同じプロンプトで来たので、計画へ圧縮せず1件1行で保持する**（過去に24件を8ステップへまとめて3件失った）。
判断は着手前の確認（4問＋2問）で確定済み。決定内容は各行の【決定】に埋めてある。

| # | 内容 | 決定・前提 |
|---|---|---|
| **W-251** | **スキルEXPの逓減が戻るまでの残り時間を `/skills` でも見たい**（現状は「下がりました」の通知だけ） | ユーザー自身「実装済みかも」。→ 実コードで裏取りしてから閉じる／足す |
| **W-252** | **木炭ブロックと圧縮木炭ブロック（×3まで）を追加し、ソースの欠片の素材にする** | 【決定】**設置できない「アイテム4段」**で追加（圧縮薪 `oak_wood_1x〜3x` と同じ方式）。木炭ブロック(=木炭9)／×1(81)／×2(729)／×3(6561)。**ソースリンク燃料の点数は素の木炭×圧縮倍率で厳密に比例させる**。ソースの欠片の儀式レシピにも既存の石炭ブロック枠と同じ比率で足す |
| **W-253** | **幸運のポーションがドロップ品質に乗っていない** | 【決定】**乗せる**。⚠ 調査済み: 釣り・拾得（`PlayerLootLuckSource`）と作業台・儀式・醸造（`CraftQualityService`）には既に乗っている。**乗っていないのはモブドロップだけ**（`PlayerMobDropBonusSource` が `mob_drop_quality` しか見ず、コメントに「幸運は意図的に対象外」と書いてある）。そのコメントごと方針転換する |
| **W-254** | **同じスレッドは同じ部位に1本まで／各スレッドを特定ステへ尖らせる** | 【決定】**1装備1本まで**（回避率・軽減率などが合計100%へ到達するのを物理的に止める）。尖らせ方は**「主軸1つ＋同系の副次を1〜2個」**。**回避・ダメージ軽減のような強いステは、それを主軸に持つ2〜3種だけが配る**。耐性・守備力・攻撃力のように「感覚で満遍なく付いている」ステは数種へ集約。⚠ `thread-sets.yml` の6段しきい値と TF 側 `ShippedThreadBandIndependenceTest` の帯目標は**重複可・2/3/4枠**前提で較正されているので、上限を1にする側とセットで引き直す |
| ~~**W-255**~~ | ~~スレッドに使用可能レベル制限を設ける（旧 W-222 / D-19）~~ | **【2026-08-25 ユーザー撤回】レベルキャップ案は取り下げ。** 本質は「上級者が集めたスレッドが新規へ無尽蔵に供給され、収集コンテンツが潰れること」の防止で、レベル制限はその手段として採らない。→ W-259 へ差し替え |
| **W-256** | **透明化がつく「隠密のスレッド」を追加する** | 【決定】追加する。⚠ `invisibility` は `threads.yml` の許可18種に入っているので新しい機構は要らない（CMD 採番・カタログ・item-stats・レシピ・リソースパックが要る） |
| **W-257** | **ソースの転送速度が一定じゃない（割合排出をやめ、階梯ごとに転送量を増やす）** | 【決定】`drain-ratio` を切って速度を一定にし、階梯の `transfer-multiplier` を **×2刻み→×5刻み**へ。**網（ドミニオンワンドの経路）にも送信元の階梯・ジャー階梯の倍率を掛ける**（今は掛からないので網だけ永久に低速）。tier1=100/20tick・tier2=500/20tick になるよう基準値も合わせる。⚠ **ユーザー報告「現状の×2が効いていない可能性がある」→ 倍率の配線を実測で確かめてから値を触る** |
| **W-258** | **ソースベリーが「満腹度が減っていないと食べられない」ためマナ回復が機能しない** | 【2026-08-25 追加報告】強みであるマナ回復が実質使えない。⚠ 調査済み: `SourceBerryListener` は `PlayerItemConsumeEvent` だけを見ており、**バニラは満腹度が満タンだと食事そのものを始めない**ので発火経路が無い。GLOW_BERRIES ベースの `BaseCustomItem`（`materials.yml` ではなくハードコード） |
| **W-259** | **上級者から新規へスレッドが無尽蔵に供給されるのを止める**（W-255 の差し替え） | 【決定 2026-08-25】**ダンジョン（構造物ルートチェスト）産のスレッドだけを魂縛**。**【2026-08-29 更新】** 対象は catalog の `bind-type`。作業台/儀式レシピがあるスレッドは TRADEABLE、レシピの無いスレッド（宝箱・ドロップ・ガチャ等）は SOULBOUND。CMD 帯ハードコードは TF 未ロード時のフォールバックだけ |

**【2026-08-25 深夜 = 8件すべて実装完了】**（残りは jar 再ビルドと配備。↓の各行が解決根拠）

| # | 解決 | 根拠 |
|---|---|---|
| **W-251** | **既に実装済みだった**（コード変更なし） | `NativeSkillTreeMenu#dailyRateLore` が `/skills` の詳細・一覧の両モードで「次の段階まで」「等倍まで」を出す（commit `7c8992a`）。**残るのは jar の再ビルド・再配備だけ** |
| **W-252** | 木炭ブロック4段を追加 | `materials.yml` に `charcoal_block` / `_1x` / `_2x` / `_3x`（`base_material: CHARCOAL`・CMD 5490-5493・3×3 相互変換）。`sourcelinks.yml` の volcanic 燃料に 45/405/3645/32805 を追加。`source_shard` の儀式レシピ2本に木炭ブロックを採用。⚠ 81倍・729倍の一括版は**台座のスタック上限(64)を超える**ので作れない（yml にコメントで明記） |
| **W-253** | 幸運をモブドロップ品質にも乗せた | `PlayerMobDropBonusSource` に `luckPerLevel` を注入し `totalBonus` へ加算。**供給はラムダ**（値を1回読むと `/trinityforge reload` で変わらない）。「幸運は意図的に対象外」という javadoc の線引きごと撤回。テスト4本追加 |
| **W-254** | 1装備1本＋主軸1/副次1へ | `DEFAULT_MAX_STACK` 2→1（**例外は `backpack` の `max: 2` だけ** ── 54スロットは「同じ防具に2本」でしか作れないので、1本にすると既存機能が黙って消える）。`item-stats.yml` の39種を**主軸+副次1**へ絞り、**強ステ7種を副次枠から全撤去**。主軸 +15%（**pbd 主軸5種だけ据え置き** ── 帯目標の余裕が 1.4pt しかなく、+15% で +66.2% になって実際に落ちた）。`thread-sets.yml` の6段は**値を5段へ足して**畳んだ |
| **W-256** | 隠密のスレッド（透明化） | fork `ThreadType.STEALTH`（CMD 300080 / `INVISIBILITY`）＋ `threads.yml`。TF は `catalog.yml` に儀式レシピ（発酵したクモの目×4 ほか）、`item-stats.yml` は**ステ節なし**（常時効果系の住み分け）、`collection.yml`・`cmd-registry.json`・`string.json`・モデルを追加。⚠ **テクスチャは `thread_assassin.png` の暫定流用**（art 待ち） |
| **W-257** | 転送速度を一定にし階梯を ×5 へ | **真因は割合排出**だった ── 配線（`effectiveMaxPerTransfer` / 階梯ブロックの登録 / 配備先 yml）は全部正しく、`SourceDrainPolicy.allowance` が `max(定額×倍率, 残量×0.25)` を採るため**バッファが増えると必ず割合が勝ち、階梯倍率が無意味になる領域へ入る**。`drain-ratio` を隣接・網とも 0.0 にし、`max-per-transfer` 50→250、`transfer-multiplier` を ×5 階梯へ（tier1=100/20tick・tier2=500/20tick）。**網にも送信元の階梯倍率を掛ける**（`NetworkTierMultiplier` 新設）。`sourcejars.yml` の11ジャーにも `transfer-multiplier` |
| **W-258** | ソースベリーを満腹時にも使えるように | バニラは**満腹度が満タンだと食事を始めない**ので `PlayerItemConsumeEvent` の発火経路が無かった。`SourceBerryConsumePolicy.needsManualConsume(foodLevel)` で排他境界（`foodLevel >= 20` のときだけ手動消費）を切り出し、`onInteract` で右クリック消費。⚠ component（`food.can_always_eat`）方式は**既に配布済みの個体が永久に直らない**ので採らなかった |
| **W-259** | ダンジョン産10種のみ魂縛 → **2026-08-29 catalog `bind-type` へ移設** | 拾得時に所有者刻印（`ThreadSoulbindListener`）／装着ゲート（`ThreadGui`）／**装着時の効果カット**（`ArmorManaListener`・`counts` 加算より前＝セット効果だけ他人に乗るのを防ぐ）／**取り外し時の所有者書き戻し**（挿して外すだけの洗浄を防ぐ）。⚠ **2026-08-29 再更新**: 作業台/儀式レシピあり = TRADEABLE、無し = SOULBOUND（`ShippedThreadSoulbindBindTypeTest`）。CMD 帯は TF 未ロード時だけ。チェスト取り出しは `InventoryCloseEvent` でも刻印 |

### 3-E. 2026-08-29 の実サーバ報告（バグ14 + 非バグ5。着手前の台帳）

**同じプロンプトで来たので、計画へ圧縮せず 1 件 1 行で保持する。** 楔の UX は【決定】修正前にプランを出す。ソースベリーは W-258 の再報告。

| # | 内容 | 決定・前提 |
|---|---|---|
| ~~**W-260**~~ | ~~金床の木材修繕が複数個だと1個分しか効かない~~ → 2026-08-29 `WoodRepairListener` が必要個数を `repairItemCountCost` に載せ、取り出しでスタック全体を消さない。`WoodRepairAnvilGuardTest` | 修繕量は `mat.durability()` を1回だけ。コストは `AnvilInventory.setRepairCost` で、シード付与は `AnvilView.setRepairCost`。スタック>1 だとバニラが成立せずコスト0＝レベル非表示。消費数も `repairItemCountCost` 未設定 |
| ~~**W-261**~~ | ~~設置魔法でドロワーがバニラの樽になる~~ → 2026-08-29 Ars `PlaceBlockPolicy` が他プラグイン PDC 付きコンテナを拒否。通常ブロックは setType → 向き → 中身転写 → BlockPlaceEvent（向きをイベント後にすると TileState が消える）。`PlaceBlockPolicyTest` | Ars `place_block`。別プラグインのドロワーは基材 BARREL。`setType` すると樽化。フォーク側 |
| ~~**W-262**~~ | ~~バックパック付き魔道装備の強化で中身消失~~ → 2026-08-29 儀式は `backpack_data` / `thread_slot_rolls` / `thread_slot_owners` を転写。TF の金床 combine・鍛冶台ネザライトも `ArsSocketCarryOver`。`ArsSocketCarryOverTest` / `netheriteUpgradeKeepsSocketedThreadsAndBackpack` | 儀式／鍛冶の結果が新規スタックで、バックパック PDC をコピーしていない。フォーク＋TF smithing |
| ~~**W-263**~~ | ~~統合版で自分の称号に当たり判定がある~~ → 2026-08-29 本人には `hideEntity`。2026-08-29 追記: Java も一人称で邪魔なので `visibleByDefault=false` + 本人以外へ `showEntity`、teleport 後も hide を掛け直す。`TitleDisplayServiceTest.othersCanSeeATitleTheOwnerCannot` | TextDisplay を Geyser が当たる実体へ翻訳。サーバは F5 を知らないので本人には出さない（三人称でも本人からは見えない） |
| ~~**W-299**~~ | ~~称号がネームタグと中心・高さが同期しない（スニーク／ジャンプで隙間が変わる）~~ → 2026-08-29 本人非表示後は先読みを外す。姿勢でオフセットが変わった tick は補間 0。`FollowLagCompensationTest` / `poseChangeSnapsTeleportDurationSoTheGapToTheNametagDoesNotStretch` | W-248 の 1tick 先読みは F5 本人用。W-263 で本人に出さなくなったので、他人から見ると称号だけ前へ出る。縦の先読みはジャンプで隙間を伸ばす。スニークのオフセットはネームタグが即変わる |
| ~~**W-300**~~ | ~~厳選の護符の対象に護符自身や品質の無いカタログ品が選べる~~ → 2026-08-29 `hasRollSeed` だけでなく `randomApplies`（品質券は `qualityApplies`）。券自身の catalog id も除外。`StatRerollTicketEffectTest` / `QualityUpgradeTicketEffectTest` / `ItemStatProfileTest` | カタログ作成は identity として必ず rollSeed を刻む。item-stats が無い特殊アイテムも「ステ持ち」扱いだった |
| ~~**W-301**~~ | ~~エンチャント運が低いうちはバニラより弱い結果にしたい~~ → 2026-08-29 運 10 未満はレベル-1（下限1）、以上は従来の格上げ。`enchant-luck.yml` / `EnchantLuckListenerTest` | 旧実装は運0でバニラ据え置き、運>0 だけ格上げ。低運の弱体化は無かった |
| ~~**W-302**~~ | ~~所有者を永続的にはがす特殊アイテム~~ → 2026-08-29 `owner_unbind_ticket`（魂縛解きの符）。owner 削除＋`TRADEABLE`。`OwnerUnbindTicketEffectTest` | `/tf bind clear` は UUID だけ消す。SOULBOUND のままだと拾得で焼き直す |
| ~~**W-303**~~ | ~~軽装備（革）が鍛冶型装飾・染色・脱色できない~~ → 2026-08-29 ガード例外＋`CatalogCosmeticPreserve`（cloneして色/trimだけ写す。stampしない）。`CatalogCosmeticPreserveTest` / `CatalogVanillaOperationGuardListenerTest` | スミスはネザライト以外を全部消していた。大釜脱色もキャンセル。染色は CraftQuality が新規 stamp |
| ~~**W-264**~~ | ~~ピストンで押したブロックを壊すと経験値が入る~~ → 2026-08-29 `PlacedBlockTracker.relocateMarks`。`PlacedBlockTrackerPistonTest` | `PlacedBlockTracker` は `BlockPlaceEvent` だけ。ピストン移動でマークが置いてけぼり／未マークの生成石が EXP 対象になる |
| **W-265** | **謎の tipped arrow**（`item.minecraft.tipped_arrow.effect.dnt_levitation` / 浮遊 00:00） | リポジトリに `dnt_` 無し。バニラの翻訳キーは `…effect.levitation`。出所は TF/Ars/EM の外（Geyser の未登録ポーションIDの可能性）。生成コードが無いのでサーバ上の他プラグイン／Geyser を見ないと消せない |
| ~~**W-266**~~ | ~~パーティクルがサーバ移動時に同期されない~~ → 2026-08-29 join 後 40tick は空キャッシュしない。`ParticleEffectServiceJoinCacheTest` | `ParticleEffectService` は join でキャッシュを消すが、HuskSync より先に空を読んで `Optional.empty()` をキャッシュする。称号側は遅延再適用がある |
| ~~**W-267**~~ | ~~広辞苑がエンチャント本になる~~ → 2026-08-29 `CatalogAnvilEnchantPreserve`。`CatalogAnvilEnchantPreserveTest` | ガードがエンチャント本適用を許可し、バニラが `BOOK` → `ENCHANTED_BOOK` に差し替える。identity（CMD/PDC）が落ちる |
| ~~**W-268**~~ | ~~「コストが高すぎます」を消す~~ → 2026-08-29 上限を外しただけではクライアントが 40 以上を決め打ちするので、表示は 39・課金は実コスト。`AnvilClientCost` / `EnchantCostReductionListenerTest` | バニラ金床上限40。`AnvilScreen` は `maximumRepairCost` を見ない。開くだけ／Prepare で上限を上げても文言は残る |
| ~~**W-269**~~ | ~~スレッドを手持ちに入れないと、重ねて同ランクが作れる~~ → 2026-08-29 未刻印ドロップは `isSimilar` でチェストの特異点へ個数加算。`ThreadItem` とホッパー刻印で max stack 1。`PickupQualityListenerArsThreadQualityTest` | 敵ドロップがホッパー経由だと拾得刻印が飛ばない。行き先スタックの NBT（特異点）が残る |
| ~~**W-270**~~ | ~~同じ部位・種類の防具を固めてスクラップすると1着分~~ → 2026-08-29 スタック数で倍化してから `remove()`。`DisassemblyListenerTest` | `DisassemblyListener` が `stack.getAmount() - 1` で1個だけ消費。地上ドロップは同種マージするのでスタック>1 になる |
| ~~**W-305**~~ | ~~バニラ金属チェストが解体で24個戻る（革は少なく見える）~~ → 2026-08-29 防具装飾の鍛冶を素材数から除外。作業台＋ネザライト強化だけ。`DisassemblyListenerTest.craftingCountIgnoresNonNetheriteSmithingSoTrimRecipesCannotInflateScrap`。**革の実測6は別件 W-307**。**8+16=24 は 1.20 の型数誤診**（1.21 は装飾18＋ネザライト強化1。装飾は `smithing_trim` で成果物が空なので `getRecipesFor` に通常乗らない） | 金属が8より多い実測は残る。型数を足す内訳は違う |
| ~~**W-307**~~ | ~~バニラ革チェストの解体が6個~~ → 2026-08-29 shape のマスを数える＋`minecraft:` レシピだけ。`DisassemblyListenerTest.shapedCountWalksEverySlotNotUniqueIngredientKeys` | `getIngredientMap` はキー1件。`getRecipesFor` にカタログの革チェストが混ざり unique-key 5→`floor(5×60%×2)=6` |
| ~~**W-308**~~ | ~~拘束が対象を空へ飛ばす~~ → 2026-08-29 `SnareEffect` が JUMP_BOOST 128 / SLOWNESS 255 をやめ、jump/move を属性 0 に。`SnareEffectExpiryTest`。**フォーク側。TF の commit には含まれない** | 1.8 の byte 溢れハックが 1.21 では Jump Boost 129 になる |
| ~~**W-306**~~ | ~~`/tf catalog` のスレッドに所有者loreが無く魂縛解きの対象にだけ出る~~ → 2026-08-29 give で catalog bind/owner を上書き。join で所有者loreを再注入。`CatalogBrowseGuiGiveBindTest` / `ItemAssemblerTest.appendOwnerLoreIfMissingAddsOwnerLineOnceAndSkipsWhenPresent` | extraCreate 成功経路は `ItemFactory.create` も `CatalogIdentity.ensure` も通らない。Ars は TRADEABLE を先に焼く。assemble 禁止のため lore が空のまま |
| ~~**W-271**~~ | ~~ソースジェム装備の軽／重が食い違う~~ → 2026-08-29 `ArmorSkillClassifier` が `use-skill` PDC を先に見る。`ArmorSkillClassifierTest` | 表記（item-stats `use-skill: LIGHT_ARMOR`）は軽。被弾EXPは `LEATHER_`/`CHAINMAIL_` 以外を全部重装に数える。ダイヤモンド基材が重装扱い |
| ~~**W-272**~~ | ~~Ars グリフ解放でエンチャント付きアイテムが吸われる~~ → 2026-08-29 `ItemCostRef.matches` がバニラ素材のエンチャント付きを除外。`countIn`/`removeFrom` は `getStorageContents` のみ（装着中の防具を吸わない）。`ItemCostRefPlainIngredientTest` | 筆記台の素材判定が Material 一致だけ。フォーク側 |
| ~~**W-273**~~ | ~~ポーション飲用後、EXPフリーザーが空瓶に経験値を入れる~~ → 2026-08-29 同tickの空瓶操作を無視。`XpBottleListenerTest.drinkingAPotionDoesNotStoreExpIntoTheEmptyBottleTheSameTick` | W-220 と同型の1クリック2パケット。飲用で手がガラス瓶に変わった2発目が格納経路へ入る |
| ~~**W-258**~~ | ~~ソースベリーが満腹でないと使えない~~（再報告） → 2026-08-25 実装済みを再確認。`SourceBerryConsumePolicy` / `SourceBerryListener.onInteract`。未配備なら jar 更新で直る | 2026-08-25 実装済み。未配備の可能性を実コードで確認してから閉じる |
| ~~**W-274**~~ | ~~スレッド魂縛の仕様変更で既存個体がどうなるか~~ → 2026-08-29 join 即刻印＋40tick 再走査、スロット変化、エンダーチェスト。`TreasureThreadSoulbindPolicyTest`。**フォーク側。TF の commit には含まれない。Ars jar 再配備が要る** | 拾得と close だけでは導入前個体と HuskSync の後書きが届かない。未刻印は `mayUse(null)` で誰でも装着できた |
| ~~**W-275**~~ | ~~エンチャント試練2のうさぎ（影渡り）のダメージが大きすぎる~~ → 2026-08-29 当初は推奨Lv20で死ぬのは仕様としてクローズ。同日 **W-304** で技を 0.70 倍し、試練の振れ型を外した | 台帳の 4.7 発は **Lv100 HP94**。推奨Lv20 では影渡り1発死。試練 `attack-power` は絶対値 |
| ~~**W-304**~~ | ~~エンチャント試練が Lv100 装備でも耐えられない／技がダイナミックLv100より痛い~~ → 2026-08-29 攻撃力は Lv100 固定のまま。`ability-damage-scale: 0.70` で技だけ弱める。試練 2/5/6/10 の `damage-modifier: 1.4` を外し AP を中央値同等へ戻した。`MobAbilityDamageScaleTest` / `ShippedBossStrengthDriftTest#enchantmentTrialsScaleAbilitiesAndDropDamageModifierVariance` | 共有テンプレの `damage-percent` は触らない。振れ型は技にも乗る |
| ~~**W-278**~~ | ~~emMob のテキストディスプレイに EM 由来名札が残る~~ → 2026-08-29 MEG/FMM 生成を抑止。同日再報告: 実機に FMM/MEG 無し。`customNameVisible=false` でも視線合わせで CustomName が出る。`NativeDisplayPolicy#applyLivingNametag` が抑止中は文字列も消す。**同日 W-309**: そのクリアが `EliteEntity.name` を消していた | TF FocusHp の下に `【100】エナジャイズドバニー`。可視フラグだけでは足りない |
| ~~**W-309**~~ | ~~EM 戦闘で `String.replace` NPE（`$bossName` / `$entity`）~~ → 2026-08-29 W-278 が CustomName を消した直後、`setLivingEntity` が `getCustomName()=null` を `EliteEntity#name` へコピーしていた。`retainStoredName`。**EliteMobs uberjar の再配備が要る**（フォーク、TF の commit には含まれない） | Java 21 は replacement=null を拒否。スクリプトと死亡メッセージ |
| ~~**W-279**~~ | ~~儀式リザルトが複数なのに editor の amount が null(1)~~ → 2026-08-29 儀式UIを `result-amount` に接続。`ritual-result-amount-2026-08-29.test.js` | Ars は `result-amount`。共通UIが作業台用 `amount` だけを出していた |
| ~~**W-280**~~ | ~~アイテムステータス補助にスレッドが出る~~ → 2026-08-29 `getItemDisplayTab` が `thread_*` / `STRING#` 帯を thread 扱い。`thread-tab-not-other-2026-08-29.test.js` | STRING の Material 推論が other。ピン退化で補助へ |
| ~~**W-281**~~ | ~~儀式で装備をアップするとエンチャント等がはがれる~~ → 2026-08-29 `ItemUpgradeCarryOver`。品質／ロールは実行者で新規。`ItemUpgradeCarryOverTest` / `RitualUpgradeCarryOverWiringTest` | 成果物が新規スタック。転写が `mage_`/`spell_book_` 接頭辞だけ |
| ~~**W-282**~~ | ~~エンチャントオーラ付き装備にエンチャント／本合成ができず、カタログで切って砥石に通すと後付けエンチャントがツールチップに出ない~~ → 2026-08-29 `ItemFactory` が `setEnchantmentGlintOverride(true)` に移行。ダミー耐久力I+`HIDE_ENCHANTS`を廃止。assemble／砥石／持ち替えで旧個体を移行。`ItemFactoryTest` / `GrindstonePreserveListenerTest` | 光沢が本物の Unbreaking I。テーブル拒否。砥石で剥がれる。フラグは all-or-nothing なので後付けも隠れる |
| ~~**W-283**~~ | ~~EXP解呪の良薬の仕様が旧（50/75/90%引き戻し）のまま~~ → 2026-08-29 並=減衰量10%削減・70%天井、上=15%削減、極=2時間無効化。`/tf decay reset` / `immune`。`ExpCleanseTonicReliefTest` / `ExpDecayAdminCommandTest` | lore・enum が「倍率の床」向け。DB は max 保存なので切り下げを persist しないと再ログインで消える |
| ~~**W-284**~~ | ~~`/tf catalog` がカタログymlの定義しか出さない~~ → 2026-08-29 Ars のプレイヤー向けIDを未分類タブへ足す。`CatalogBrowseGuiTaxonomyTest.extraCustomIdsLandInUnsortedWithoutDuplicatingCatalog` | `itemCatalog.all()` だけ。素材・スレッドは catalog エントリがあるから見えていた |
| ~~**W-285**~~ | ~~装備パーティクルの螺旋が止まっていると左右2本のギザギザ~~ → 2026-08-29 位相を実時間で回し、出荷 `particle_petal_ring` を 3点→12点。`ParticleShapeGeometryTest.helixPhaseRotatesInPlace` / `threePointThreeTurnHelixWithoutPhaseIsTwoVerticalLines` | 3点×3巻きは位相0だと XZ が (+r,0)/(-r,0)/(+r,0) のみ。動いていると残像で螺旋に見える |
| ~~**W-286**~~ | ~~インベントリ内ドラッグでゴースト（見た目だけの増殖）や飛び~~ → 2026-08-29 掴む／置く／ドラッグでは翌tick走査しない。確定済み品質のスロット変化も走査しない。`PickupQualityListenerTest.pickupClickDoesNotSweepBecauseItWouldGhostDuringDrag` | `PickupQualityListener` がクリック／ドラッグの翌tickに `setItem`／`setItemOnCursor` してクライアント予測と衝突 |
| ~~**W-276**~~ | ~~楔系特殊アイテムの UX~~ → 2026-08-29 メインハンド所持＋ノード／スキルクリックでプレステージと同じ確認GUI。押したマスに「はい」を置かない。`NativeSkillTreeMenuFunctionItemConfirmTest` | 持っているだけでは発動しない。確定は確認画面の「はい」 |
| ~~**W-277**~~ | ~~ソースリンク全種の倍率を 2,4,8,…,512 から 2,3,6,9,18,27,54,81,162 へ~~ → 2026-08-29 **生成量 (`yield-multiplier`) だけ**張り替え。転送は W-257 の ×5 階梯のまま。説明文の「転送レート xN / 生成量 xN」も実値に同期。`ShippedSourceLadderTest.loreMultipliersMatchConfigValues` | W-257 で ×5 階梯に変わっているので、現行の実値を読んでから書き換える。フォーク yml |
| ~~**W-287**~~ | ~~`Block-attached entity at invalid position`~~ → 2026-08-29 連鎖破壊の `setType(AIR)` が額縁等を残す穴を塞いだ。`ChainBreakSupport#popAttachedHangings`。固定座標で残るなら構造物の額縁（MC-252934） | Paper 1.21 は支持ブロックが無い hanging を ERROR ログする。一括採掘／伐採は BlockBreakEvent を飛ばない |
| ~~**W-288**~~ | ~~既存スレッドが表更新後も古い lore のまま~~ → 2026-08-29 品質と pt を保ったまま `ThreadItem#refreshLoreKeepingIdentity`。`ItemRefreshListenerTest.staleThreadKeepsSeedAndQualityAndNeverGoesThroughAssemble` | `assemble` はスレッド lore を壊す。`restampWithQuality` は seed を新規発番する |
| ~~**W-289**~~ | ~~アイテムステータス補助に宝箱スレッド10種が出る~~ → 2026-08-29 `item-stats.yml` の `STRING#300070-300079` を thread にピンし戦闘系ネストへ。`thread-tab-not-other-2026-08-29.test.js` | W-280 の JS 身元判定だけでは旧プロセス／ピン無しカードが補助へ残る。ステ定義は移さず表示タブだけ |
| ~~**W-290**~~ | ~~詠唱効率スレッドで消費マナが 1 になる~~ → 2026-08-29 分数 0.15 と percent-point 15 を同じ 15% に揃える。`ThreadType.SPELL_COST_DOWN` 既定 10→0。`SpellManaCost` / `ThreadManaStatRoutingTest` | item-stats の 15 を ×100 すると 1500→キャップ100%。enum 10% と item-stats の二重加算もあった |
| ~~**W-291**~~ | ~~圧縮フグ・熱帯魚・ハチミツブロックを 729 倍まで~~ → 2026-08-29 Ars `materials.yml` に `_1x/_2x/_3x`。CMD 264-272。9倍だけ `food-gimmick.yml` に登録 | 圧縮は catalog ではなく materials。81/729 は解凍して食べる既存規則 |
| ~~**W-292**~~ | ~~魔法バインド/アンバインド対象がメインハンドやホットバーへ落ちる~~ → 2026-08-29 `SpellBindTargeting`。オフハンド占有時はフォールバックしない。unbind コマンドも同じ。`SpellBindTargetingTest` | GUI はオフハンドが無効だとスロット8へ落ちていた。unbind コマンドはメインハンド固定だった |
| ~~**W-293**~~ | ~~醸造の見習い習得後に効果反転ができなくなる~~ → 2026-08-29 品質で倒したバニラ由来だけ `VanillaPotionInvert` で救済。解放式カスタムは W-116 のまま止める。`PotionQualityListenerTest` / `VanillaPotionInvertTest` | 見習いで `potion_quality_bonus` が乗り base が WATER になる。クモの目はバニラが水→弱化しか見ず、W-116 が醸造ごと止めていた |
| ~~**W-294**~~ | ~~装備が壊れたとき装着スレッドがドロップせず消える~~ → 2026-08-29 `SocketedThreadReturn` / `ThreadBreakDropListener`。TF 手書き破壊は `ItemBreakSignal`。`ThreadBreakDropWiringTest` / `EquipmentDurabilityServiceTest` / `ChainBreakSupportSingleRollTest` | スレッド実体は装備 PDC のみ。バニラ破壊も手書き `setItem(null)` も返却しない。GUI 取り外しと同じ組み直しが要る |
| ~~**W-295**~~ | ~~金床が39表示でも結果空／スレッドに editor 外のグレー数値／セット効果とフレーバーの体裁~~ → 2026-08-29 表示の39は次tick（結果を戻してから）。`ThreadType` マナ数値0、yml 未記載は0。セットは `Nセット`＋全角インデント、フレーバー1行紫。持ち替えで lore 再組。`EnchantCostReductionListenerTest` / `ItemRefreshListenerTest.currentGenerationThreadStillRefreshesLoreOnHold` / `ThreadManaStatRoutingTest` / `ThreadLoreFormatSourceTest` | Prepare 中に39へ落とすと Paper が実コスト≥40で結果を空にする。enum 既定が item-stats に無い「マナコスト -10%」等をグレーで足していた |
| ~~**W-296**~~ | ~~エンチャント／合成で装備の品質ptとロールが消える~~ → 2026-08-29 金床 combine とネザライトは合成元の quality+rollSeed。本付けは `ItemIdentityCopy`。テーブルは Prepare のスナップショットから戻す。`CatalogAnvilListenerTest` / `CatalogAnvilEnchantPreserveTest` / `EnchantLuckListenerTest` / `ItemIdentityCopyTest` | 合成は毎回新規 seed。本付けは type 不変だと PDC が落ちる。品質キー欠落は `quality()` が 0 |
| ~~**W-297**~~ | ~~醸造品質が強度を上げ、時間が分かりにくい~~ → 2026-08-29 強度加算を削除。持続は 0.1pt あたり +1%（0 未満は短縮）。`alchemy-quality.yml` / wiki 03・13・15。`PotionQualityListenerTest` / `AlchemyQualityConfigTest` | 旧: 1pt=+1秒・2ptで強度+1。ユーザー決定で強度は仕様ごと削除 |
| ~~**W-298**~~ | ~~重装備の移動デバフが無いに等しく、回避が付いている~~ → 2026-08-29 全部位 `move-speed: -0.05`、回避削除、防御系 ×1.10。wiki 03。`ShippedHeavyArmorIdentityTest` | 旧: 部位ごとに −0.2〜−0.4%。回避は軽装の役割 |

---

## 3.5 配備・運用待ち（ユーザー実行。エージェントは `D:\game` に書けない）

**手順の型: サーバ停止 → jar 配備 → config 配備 → リソースパック → 起動 → 起動後コマンド。**
統合版がらみを含む一括は `ops\launch\deploy-full.cmd`（GeyserExtra → TF の順を固定済み）。

1. **jar 配備**（停止後）:
   - TF `TrinityForge/build/libs/TrinityForge-0.1.0-SNAPSHOT-all.jar`（16,640,500 bytes / 08-25 00:44。W-216 良薬・W-210〜213・**W-218 スキルツリーの配置**・**W-214/215 パーティクルシードの金床付与と一覧**・**W-221 消すシードと lore 表示**・W-204〜206・盾 W-163・図鑑 W-157/158 ほか多数の未配備修正を含む）。**⚠️ 2026-08-25 15:18 に配備された jar が入っている**（08-24 02:26 のものではない。ただし**その 15:18 の jar より新しい修正が下に積まれている**ので、下記の再ビルドは必要）。かつて **08-24 02:26 に配備されていた jar は `394ec97`（一括伐採の追加ドロップ）より 18 分古かった** ── リンゴ/金リンゴが一括伐採で出ないのはこれが理由なので、この配備で解消する。`836b9b6`（経験値瓶の反転）も同様に**再ビルドしてから**配る
   - **⚠️ 2026-08-25 午後〜夕方の TF 側修正（W-242〜245 = パーティクルの発生位置・発生パラメータ・形状・農業EXPのスニーク連打／**W-247〜249 = 追従の1tick先読み・粒子の量の抑制・軌跡の形状／**W-250 = 付呪と総合のノードのつながり方**）は上の jar より新しいので、配る前に必ず再ビルドする。** config も同時に配ること（`progression/special-rewards.yml` に新キーを書いた。未定義でもコード既定で動くが、増えた形状の実例はこの yml 側にある）
   - ArsPaper `fork-handoff/arspaper/fork/build/libs/ArsPaper-1.0.0.jar`（1,211,100 bytes / 08-24 12:54。W-209 グリフ画面・W-210/211 ソースリンクほか）。**✅ 2026-08-25 10:53 に配備済み**（配備先の jar を開いて「詠唱の耐久消費を触媒だけに限る」fork `7c383c6` が入っていることを確認した）。**⚠️ ただしその後の fork `c71565b`（網の転送も割合排出にする）は未ビルド** ── ソースリンクの転送が毎秒2.5点で止まる件の修正なので、次の停止時に再ビルドして配る。**config 配備は不要**（新キー `transfer.network.drain-ratio` は未定義でもコード既定 0.25 で動く）。Java 側の修正は**配備先の yml を直しても再起動しても反映されない**
   - **⚠️ ArsPaper の yml は「プラグインの version 文字列が変わったときだけ」再抽出される**（`ArsPaper#updateResourceFiles`。配備先 `.version` = `0.1.0-SNAPSHOT` = jar の `paper-plugin.yml` と一致）。したがって**フォークの出荷 yml を直しても jar 差し替え・再起動では配備先に届かない**。届けたい場合は配備先のファイルを直接編集して `/ars reload`。逆に、将来 version を上げた瞬間に配備先で直接直した値が `.bak` へ退避されて出荷値に化けるので、**出荷 yml 側を必ず追随させておくこと**
   - **設定エディタを再起動する**（W-246。サーバ停止とは無関係にいつでも可）: 走っているプロセスは 08-21 起動で、**形状の追加を知らないまま保存を拒否する**。`tools/config-editor/start-config-editor.bat` で入れ替え、**開いているページはリロード**（`public/js` も変わっている）。⚠ リロードすると**保存していない編集は消える**ので、保存したい編集があるなら再起動→保存→リロードの順で
   - **Dev_Server のスレッド枠拡張の儀式を直す**（K-40）: `powershell -NoProfile -ExecutionPolicy Bypass -File ops\scripts\fix-thread-slot-tiers.ps1`（`-WhatIfOnly` で下見。2026-08-25 の実測では Main/Resource は既に正しく、**Dev だけ Ⅱ=1/Ⅲ=3**）。実行後は `/ars reload` で反映（再起動不要）
   - EliteMobs uberjar（W-54③・W-71 宝箱/共有ルート・W-80 ダンジョン。**配備前に全同梱 uberjar であることと鮮度を確認**）
   - GeyserExtra paper jar ＋ extension jar（08-23〜24 の統合版修正一式: 焼きアイコン・台帳修復・展開競合・レシピ注入 NPE）
   - PixelRank `rank/build/libs/PixelRank-1.3.jar`（W-64 移動距離）
2. **config 配備**: `ops\launch\deploy-config-head.cmd`（W-66 ネザー帯・W-72〜77 武器再較正・W-73/W-80 `damage.yml`・EM 役割逆転の `mob-overrides.yml` ほか）
3. **リソースパック**: `ops\scripts\set-resource-pack.ps1 -Url <pack-20260824121454 の URL> -Sha1 4e9748ecb84bf057853f5c9efab447c5d91a9de3`（過去バッチの sha1 指定はこれで置き換え）。統合版は次回接続時に再取得
4. **起動**: `ops\launch\set-rcon-env.cmd` → `deploy-launch.cmd`。資源サーバのリセットは `reset-resource.cmd`（`RESET` 入力で本実行）
5. **起動後コマンド**:
   - `/trinityforge reload`（reconciler は reload の中でしか走らない）
   - **SP 補填（W-193 の旧プレステージ減衰ぶん。対象がオンラインのとき）**: `/trinityforge progression level <name> POWER add <n>` — **残り Sora0608 2 / .NAGIdayo5655 3 / Rando4649 4**（Kuragemal 5 と ame3398610 7 は 08-24 10:54〜11:03 に実行済み＝サーバログで確認）。**これは W-213 の返却とは別物**（こちらは POWER EXP の取りこぼし、W-213 は消えたノードへの支払い）なので**両方やってよい。二重補填にはならない**
   - HuskSync: **Sora0608 の 08-23 21:06:38 以前のスナップショットを pin → restore**
   - WorldGuard: `ops\scripts\apply-mob-griefing-guard.ps1` → `/wg reload` → `/gamerule mobGriefing true`
   - W-27①: `/em language japanese`／W-46: Dev_Server で `/nightbreaklogin <token>`（トークンは nightbreak.io/account）
6. **個別スクリプト・設定**（停止中に）:
   - W-67: `ops\launch\swap-deathchest.cmd -Download`（DeathChest → AxGraves 差し替え）
   - W-156: 岩盤破壊フラグの設定変更スクリプト → 再起動
   - W-94: エリート化抑止の設定スクリプト
   - W-201: `ops\scripts\apply-infinity-core-to-functional-items.ps1`
   - W-65: PixelRank の Main_Server `server-name` を `main` へ
   - W-157: DiscordSRV `AvatarUrl: "https://crafthead.net/helm/{texture}/{size}"` → `/discordsrv reload`
   - W-24: `plugins/EliteMobs/trinityforge.yml` の `currency-shower` / `boss-unique-loot` / `treasure-chest-loot` / `arena-loot` を false
   - W-14: 配備先 `items/material-lists.yml` の `iron_axe_tool` を editor で修正（おそらく `iron_axe_tf`）
7. **配備後の実機確認**:
   - **W-213 の SP 返却（自動。コマンド不要）**: 新 jar で起動した瞬間に `SkillTreePerkPruner` が走る。起動ログに
     `[skilltree] loaded 16 tree(s) OK` が出たうえで `[progression] 消えたノードのperkを 30 件剥がし、SP 30 点を 14 人へ返却しました` が出れば成功。
     `孤児perkの掃除を見送りました` が出た回は**ツリー yml が壊れていて安全弁が働いた**ので、原因を直して再起動（放置しても悪化はしない）。
     返却先: HinataS2010 4 / .kenntoaya 4 / Rando4649 3 / .yuzu3850 3 / .taputeru 2 / Sora0608 2 / .NAGIdayo5655 2 / haru_harura 2 / .shizurei555 2 / .natsuking003 2 / Kuragemal 1 / Eroder256 1 / .tomi0071 1 / .pale4780 1。
     伐採 `A-2-1`〜`A-2-4` は現行の `B-2-1`〜`B-2-4`（効率伐採Ⅰ〜Ⅳ）へ**付け替えではなく SP で戻す**ので、対象者は取り直しになる。
   - W-44（起動ログから `failed to register recipe for 'key_binder'` が消えたか）／W-158/W-160/W-161（統合版のクラフト・鍛冶台・ちらつき）／統合版レシピブック増殖の再現（`0c4feb3` の修正）／W-27③ 虚空右クリックでガチャ券・鍵が使えるか

---

## 4. 既知の未修正の問題・弱点

いずれも**意図的に許容している**か、**直すには判断が要る**もの。新規に見つけたバグはここへ足す。

| # | 内容 | 判断 |
|---|---|---|
| **K-40** | ~~**スレッド枠拡張の儀式Ⅱが「死に儀式」になる値が未コミットで乗っている**~~（2026-08-25、詠唱耐久の修正でフォークのテストを全走して発覚） → **出荷 yml は解決**（ユーザー判断「Ⅰ=1 / Ⅱ=2 / Ⅲ=5 に戻す」、fork `1760404`。同じ未コミット差分に入っていた儀式 `zombie_villager_summon` も一緒に確定した。フォーク 570/失敗0/skip0）。**残るのは Dev_Server の配備先だけ** → §3.5 の `ops\scripts\fix-thread-slot-tiers.ps1`（Main/Resource は元から正しい） | ArsPaper fork の `src/main/resources/items.yml` に**未コミットの編集**（設定エディタ由来。同じ差分に儀式 `zombie_villager_summon` の新規追加が入っている）が乗り、`max-slots` が **Ⅱ 2→1 / Ⅲ 5→3** になっている。HEAD (`c97d35e`) は Ⅰ=1 / Ⅱ=2 / Ⅲ=5 で正しく、**説明コメント（「累計 2 まで解放」「累計 5(=カテゴリ上限と同値)まで解放」）は書き換わっていない**ので値と食い違っている。`max-slots` は**その装備に刻まれた儀式由来スロットの累計上限**（`ItemData#ritualThreadSlotBonus` はアイテムの PDC）なので、Ⅱ=1 だと Ⅰ を1回回した装備では Ⅱ が永久に失敗する（素材もソースも消費せず、ログにも何も出ない ── `docs/agent-context/forks-and-mobs.md` が明記している既知の壊れ方）。`ThreadRitualRecipeConfigTest#slotExpandMaxSlotsAreStrictlyIncreasing` が赤なのはこれ。⚠️ **配備先は本番だけ無傷**: Main_Server / Resource_Server の `items.yml` は Ⅰ=1 / Ⅱ=2 / Ⅲ=5 のままで、**Dev_Server だけ 1/1/3**。⚠️ **ArsPaper の yml は「プラグインの version 文字列が変わったときだけ」再抽出される**（`ArsPaper#updateResourceFiles`。配備先の `.version` = `0.1.0-SNAPSHOT` = jar の `paper-plugin.yml` と一致）ので、**jar を差し替えても再起動しても配備先の値は動かない** ── Dev を直すなら配備先のファイルを直接編集する。ユーザーの意図した調整かを確認してから触る |
| K-1 | **同一地点EXP逓減が友好モブの養殖場を数えない** | カウンタはバニラEXP書き込み経路で回るため、レベル帯の対象外である C 群（友好モブ 39 種）には反応しない。TT の本命ではないので許容 |
| K-2 | **ATTRIBUTE チャネルの上限は「実効値」の上限ではない** | `move-speed` / `attack-speed-bonus` / `attack-reach` / `knockback-resistance` / `max-health` の上限は「**TF が要求する寄与分**」に掛かる。Haste や他プラグインの寄与は含まない。`PerkAttributeApplier` の「ライブ属性値を読まない」原則を守るための意図的な線引き（→ `docs/config-reference/combat/stat-caps.md`） |
| K-3 | **editor で yml 本文のコメントが保存時に消える** | `tools/config-editor/lib/yamlio.js` の仕様。実害は継続中（`item-stats.yml` で 65 行、`collection.yml`・`catalog.yml` でも確認）。**1 件ずつ復元しても次に同じ画面を保存すればまた消える**。恒久対策は editor 側でコメントを保持すること。当面は説明を `docs/config-reference/` へ退避し、yml 本文に長いコメントを書かない |
| K-5 | **ステータスのトリガー/発動制限の宣言** | 段階 1〜4 完了（宣言スキーマ＋拘束テスト＋`/tf stats detail <キー>`）。未宣言キーは理由コメント付きで `LoreConfigDeclarationTest.UNDECLARED_ALLOW_LIST` に固定＝**新しいキーを宣言なしで足すとビルドが落ちる**。プラン全文はアーカイブ |
| **K-39** | **カタログ品がバニラレシピから弾かれるとき、理由が一切表示されない**（2026-08-18 ユーザー質問「ディスペンサーの件、類似バグ他にない?」の調査で判明） | `CatalogWorkbenchListener` は「カタログ品を消費できるのはオプトインした TF レシピだけ」という規則を持ち、外れると**結果枠を空にするだけ**でメッセージを出さない。**この失敗の仕方が「レシピが壊れている」と区別できない**のが問題で、ディスペンサー報告（TF の弓が締め出されていた）が実バグとして上がってきたのも、糸スレッドを弓レシピに置いた人が同じ報告を上げるのも、現地では全く同じ見え方になる。**2026-08-17 の修正でスタック不可（＝装備・道具）のカタログ品は素通しになった**ので、残るのは**重ねられるカタログ品**だけ: 本4種（本棚/エンチャント台/書見台/本と羽根ペン）・糸スレッド6種（弓/羊毛/釣り竿/クロスボウ/リード）・アメジスト3種（色付きガラス/望遠鏡/較正済みスカルクセンサー）・グロウストーンダスト・エンダーアイ（エンダーチェスト/エンドクリスタル）・海洋の心（コンジット）・エコーシャード（リコールコンパス）・陶器の破片多数（飾り壺）・鍛冶型テンプレート多数（テンプレート複製）。**これらは「素材として溶かされない」ことが正しい**（貴重品を素の糸として消費させない）ので**挙動の修正は不要**だが、**弾いたときにチャットで一言出す**だけで同種の誤報が止まる。※ `TRIAL_KEY`・`NAME_TAG` はクラフト用途が無く、`RABBIT_FOOT`・旗の模様は醸造／織機なのでこのガードの対象外 |
| **K-14** | **リソースパック配線が未了（副作用で 7 件が別アイテムに化ける）** | 27 枚の PNG は参照元（`models/item/*.json` と `assets/minecraft/items/*.json`）が無いので描画されない。既存 items json が張られている 7 件は**素のバニラではなく別アイテムの見た目で出る**（ピリジャーの鎧片が「重金属」、深淵/束縛者の弓・メイス・トライデント計 6 種が「ソースジェム武器」）。441 CMD 中 299 が未配線。**統合版のアイテム名は TF パックの `texts/*.lang` では出ない**（GeyserExtra が `custom_items.json` の `display_name` から自動生成する側が効く。詳細 = `docs/agent-context/bedrock-geyser.md`） |
| **K-15** | **ガチャ券 6 種のテクスチャがリポジトリにも配備先パックにも無い** | **2026-08-10 に範囲を縮小**（→ J-14）。「18 種」のうち**コア 5 種とモブ素材 13 種は既に配線済み**で、残るのは `gacha_ticket_0`〜`_5`（materials.yml CMD 5011-5016）だけ。PNG 自体がリポジトリに無いので**絵が来るまで着手できない**。置き場所は J-14 に確定済み |
| K-16 | **ソース生産レートを上げる階梯**（部分前進） | 2026-08-03 に `yield-multiplier`（生成量）と `transfer-multiplier`（転送）で階梯 IV/V まで入り、**素材効率は階梯で改善するようになった**。ただし到達時間が「ソースリンクを何台並べたか」で決まる構造自体は残る。**バッファの int オーバーフロー**は `long` 受け＋`Integer.MAX_VALUE` 飽和で解消済み |
| **K-17** | **ロール限定コンテンツが乏しい** | ロールの効果はステ注入・EXP 倍率・常時ポーション・ヘイト係数の 4 種。2026-08-02 に `use-role` とロール専用装備 8 点が入ったので全否定ではないが、**`use-requirements` の required-role や drops のロールゲートが無い**ので「この職でないと作れない／出ない」は依然として無い |
| **K-18** | **厳選に「育てる」側が無い** | ランダム抽選と振り直しまでは成立。**強化レベル（+0→+20）／サブステ 4 本の段階解放／部位別の主ステ固定／ロック／スコア表示／一括分解**が全て無い。個体ごとに PDC が違うので**スタックせず周回でインベントリを圧迫する** |
| **K-19** | ~~**`stats/stat-caps.yml` が空なので厳選の上限が効かない**~~ **方針確定（2026-08-16 ユーザー決定）: 上限なしが正** | 「天井があるとそこでゲームが終わるから要らない。天井なしでバランスの取れた設計にする」。出荷 `combat/stat-caps.yml` の `stat-caps: {}` は意図された状態。`ShippedStatCapsDriftTest` は「上限必須」を廃止し「書かれた場合のみ単品最大との整合を検査」へ再仕様化済み。**残タスク: thread-rolls の抽選幅見直し**（19枠フル厳選で crit-chance 171% は 100% 超が死に値）と `docs/config-reference/combat/stat-caps.md` の旧上限値記述の更新 |
| **K-22** | **束縛者が「最強」として設定されていない／第1目標が実質「最後の目標」** — **(1)(3) は解決済み（2026-08-16 確認）** | ~~(1) 束縛者 18 体に `level` / `max-health` / `attack` が 1 つも無く全モブ共通ランプ任せ~~ → 08-01 に `mob-overrides.yml` へ 18 体の max-health/attack-power を明示済み（`ShippedBossStrengthDriftTest` が pin）。~~(3) `dungeon/gates.yml` の入場ゲートが空~~ → `em_id_binder_of_worlds` に `key-item: key_binder`＋`required-combat-level: 40` 設定済み（key_binder は印5個から合成＝前段踏破の強制も兼ねる。レシピ登録は W-44 修正で復旧）。残: (2) `goal_worldbinder` の parent が `delve_all_seals` である点のみ（設計判断） |
| **K-23** | **NETHERITE メソッドの 13 件が Bukkit へ一切登録されない**（旧 U7） | `RecipeSpec.isBukkitCrafting()` が workbench/inventory だけ true で `CatalogRecipeRegistrar.java:95` が弾く。リポジトリ全体で `new SmithingTransformRecipe` は **0 件**。影響: `netherite_bow` / `netherite_trident` / `netherite_mace` / `netherite_crossbow` |
| **K-25** | **儀式の素材別EXPと `custom:` 素材表の 12/13 行が死んでいる**（旧 U1/N6） | `CraftQualityListener.java:227-236` の `materialToken` が TF カタログ PDC しか読まず Ars の `arspaper:custom_item_id` を読まない。素材表は作業台側にしかない。**2026-08-04 に「消費ソース量に応じた追加EXP」（`ars-smithing.exp-per-source`）が入って定額一辺倒ではなくなったが、素材表を参照しない点は未解決** |
| **K-26** | **召喚馬のインベントリを守る仕組みが無い**（旧 U14） | `SummonSteedEffect.java:85` が `setSaddle(new ItemStack(Material.SADDLE))`。保護は fork に 0 件 |
| **K-27** | **ArmorStand/Mannequin の除外が EXP 経路に無い**（旧 U10） | 除外は `DamagePopupDisplay.java:67` と `FocusHpDisplay.java:191,205` の 2 箇所だけ。現在の唯一の防波堤は `skill-exp.yml` の `unlisted-entity-multiplier: 0`。**未検証: ArmorStand で `EntityDeathEvent` が実際に発火するか**（発火しないなら 1.0 に戻しても再発しない可能性） |
| **K-28** | **レシピブラウザの並べ替えに品質・5 分類が無い**（旧 N4） | `SortMode` は NAME/KIND/LEVEL/DEFAULT のみ、`KindMode` は ALL/WORKBENCH/RITUAL のみ。`RecipeBrowserGui.java`(74KB) に `quality`/`品質` の出現 0 件 |
| **K-29** | **束縛者 HP 29,106 が Spigot 既定 `attribute.maxHealth.max`(1024) を超える** | 現行サーバは設定済みなので出ていないが、**新設サーバでは無言でクランプされる**。運用側の注意点 |
| **K-30** | **GOLD 帯（`use-level-requirement` 35）で重武器が弱い** | H/L が 0.89〜0.95。全帯 `damage-modifier` が揃っている構造的なもの。LOW |
| **K-31** | **U3（ドリリングでツルハシのモーションが消える）= inconclusive** | 描画事象なのでリポジトリ内では確定不能。実機でしか切れない |
| **K-32** | **editor の「素材タブへ移動」が `external-source` を引き継がない** | `moveEntryToMaterials` がエントリを作り直すため。`ShippedCatalogExternalSourceDriftTest` が赤くなって気づけるので未対応 |
| **K-33** | **フォーク側 `ThreadGui#isEffectThread` の PDC 2 種判定にテストが無い** | TF 側からは検証不能。フォークは Bukkit ランタイムを持たないのでソース検査になる |
| **K-34** | **editor のフォーム欄内に横あふれが 11 件残っている** | `ops/scripts/editor-overflow-audit.mjs` が 112 通り（8 幅 × 14 画面）で検出。**すべて `mainOver` 0 = ページ本体は横スクロールしない**ローカルなあふれ（フォーム欄内で 22〜48px）。箇所はカタログ武器/素材・モブ定義・スキルEXP獲得。レスポンシブ化時点（`fdb364e`）では 0 件だったので、その後 dev に入った 20 コミットぶんの新しい長文ラベル・追加フィールドで生えた。LOW |
| **K-35** | **モブ側 config は `PercentStatNormalize.coerce` を通らない**（2026-08-16 K-10 全数調査の副産物） | `coerce`（「20 → 0.2」矯正）の呼び出し点は `BaseStatsConfig` / `ItemStatsConfig` / `RewardFieldsParser` / `RoleBuffsConfig` / `SkillTreeConfig` の6箇所のみで、`MobTypesConfig` / `MobProfileConfig` / `MobOverridesConfig` / `RampParser` は `getDouble` の生値。**モブ yml に `crit-chance: 20` と書くと 2000% として素通りする**。現行の出荷 yml は fraction 表記なので実害は未発生だが、editor での将来の編集で踏む穴。修正はモブ系パーサへの coerce 追加（プレイヤー側と同じ 2..100 整数ルール）だが、モブの非レートステ（attack-power 等）に誤爆しないよう RATE_KEYS 限定で掛けること |
| **K-36** | **`ScribingTableGui.getUnlockedGlyphs()` だけ JSON パースが try/catch 無し**（2026-08-16 グリフキー全走査の副産物） | 同キーの読み手 8 箇所のうち、ここだけ `JsonParser.parseString(json).getAsJsonArray()` を素で呼ぶ。PDC が壊れると `render()` から例外が上がり**筆記台 GUI がそもそも開かない**。K-37 の書き手を撤去したので壊れたデータが生まれる経路は消えたが、最弱点であることは変わらない。他の 7 箇所と同じく「catch して空集合へ倒す」を足すのが妥当（別タスク） |
| **K-38** | **EliteMobs の `build.gradle:74/105` が廃止済みの PlaceholderAPI リポジトリを指している** | `https://repo.extendedclip.com/content/repositories/placeholderapi/` は 404、指定の `2.10.9` も配信されていない（現行は `/releases/`、最古 2.11.5）。ローカルキャッシュで通っているだけなので、**キャッシュが飛ぶと EM がビルド不能**になる。TF 本体は 2026-08-16 に `/releases/` + `2.11.6` へ移行済み |

---

## 5. 恒久的な注意事項（繰り返し踏んでいるもの）

**「常時効果スレッドは反映されません」は仕様（2026-08-25 確定・旧 W-56）:**
スレッド装着 GUI が武器・触媒・採集道具に対して出す警告で、条件は
**「対象が防具でない」かつ「そのスレッドが常時ポーション効果・飛行・バックパックのいずれかを持つ」**。
2026-07-31 の設計判断で、数値ステ（マナ・厳選ステ・セット効果）は防具以外へも広げた一方、
**常時ポーション効果と飛行は着用防具に限った** ── 剣を握っただけで滑空が付いたり、
ホットバーをスクロールするたびに効果が点滅したりするのは機能ではなく不具合に見えるため。
集計側（フォークの `ArmorManaListener`）も同じ区分で「着用防具由来のときだけ」効果を集める。
**バグではないので直さない。** 同じ質問が来たらこの行を示す。

**残タスクに載せてよいものの線引き（ユーザー指示・恒久ルール）:**

- **「機構は実装済みで、config に値が入っていないだけ」のものは残タスクにしない。** 握りつぶす。
  運用上どう設定するかはユーザーの裁量。例: 触媒のオフハンド運用（`offhand-stats-apply` は既に設定可能）。
- **死にステータスは 2 種類に分けて扱う:**
  - **配線は済んでいて config が未設定なだけ** → 握りつぶす。
  - **配線が無いのに config や計画にパラメータとして存在する** → 残タスクにする
    （「設定できるのに効かない」＝プレイヤーを騙す状態）。該当例 = K-9。
  - **この型で起票する前に必ず実コードで裏を取る。** 旧 W-12（エンチャント消費EXP軽減）は
    実装も語彙も既にあり、記録のほうが古かっただけだった。
- **ユーザー報告の「UI が崩れる」は判断待ちではなく実バグ**として扱う。

**バッチ受領時は全件を先に登録する（2026-08-05 の取りこぼしを受けた恒久ルール）:**

- 実サーバ報告が複数件まとめて来たら、**着手前に 1 件 1 行で §3 へ登録する**。
  計画を立てるより先にやる。計画は項目をまとめる（＝粒度を落とす）ので、
  **計画だけを進捗の台帳にすると、まとめた時点で落ちた項目が二度と現れない。**
- **完了報告は計画のステップ単位ではなく、ユーザーが書いた項目単位で出す。**
  未着手の行を省略しない。「N 件中 M 件完了、残り M' 件は未着手」と必ず明示する。
- 実害の実例: 2026-08-04 のバッチ 5（24 件）を 8 ステップの計画へ圧縮した結果、
  **3 件が計画にすら載らず、9 件が 2 行にまとめられた**。10 件を直して配備した時点で
  「配備完了」とだけ報告したため、ユーザーからはバッチ全体が終わったように見えていた。
  →W-28〜W-40。
- **2026-08-05 時点で配備待ち（ユーザー作業）**: サーバ停止 → ①jar（`ops\launch\deploy.cmd`。
  `--config` は付けない）②config（`ops\launch\deploy-config-head.cmd`）。
  jar は 2026-08-05 21:38 時点でビルド済み・検証済み（TF `build/release/TrinityForge-all.jar` に
  `SkillExpBonusKeys.class` あり／ArsPaper jar は `6e64216`（スレッド lore）より 2 分古かったので再ビルド済み）。
  **稼働中の jar 差し替えは必ず `NoClassDefFoundError`** なので必ず停止後に行う。
- **config 配備は `deploy-config-head.cmd`（2026-08-05 新設）を使う。** 他の 2 つはこの状況で外れる:
  `deploy.cmd --config` は**ワーキングツリーを配る**＝他セッションの未完成 yml を出荷する。
  `deploy-config-skip-wip.cmd` は**未コミットのファイルを丸ごと除外**するので、
  **自分の変更と他人の WIP が同じファイルに乗っていると自分の変更まで落ちる**
  （2026-08-05 の `stats/lore.yml`＝職業EXP増加 12 件の表示定義がまさにこれ）。さらに
  **ArsPaper フォークは外側リポジトリから `.gitignore` 除外なので `git status` が何も返さず、
  フォークの未コミット変更（`materials.yml` のレシピ変更）を検出できない**。
  新スクリプトは除外方式をやめ、`ops\scripts\export-head-config.ps1` で
  **HEAD（フォークは自前リポジトリの HEAD）を `tmp\deploy-head` へ展開してそこから配る**＝
  「配るのはコミット済みの状態」と定義する。誰の編集途中も混ざらず、
  コミット済みの変更が同居のせいで落ちることもない。**今回配らなかった未コミット yml の一覧も毎回表示する**。

git 系:

- **`Klee319/trinityforge` は PUBLIC。** 作業ブランチは `dev`、`main` はユーザーの明示指示があるときだけ。
  **push 前に `gh repo view --json visibility`**（TF 本体 jar を意図せず公開した事故がある）。
- **`git add -A` / `git commit -a` / パス指定なしの `git add --renormalize` を使わない。**
  並行セッションが同一ワークツリーで走るので、他人が**実装中**のファイルを巻き込む。
  2026-07-27 に実際に発生（docs 名義のコミットに set-buffs 移行のソースが丸ごと入った）。
  **同一ファイル内で混ざっている場合**は、HEAD の blob へ自分の変更だけを差し込んで index へ直接書く
  （手順は `docs/agent-context/ops-build-deploy.md`）。
- **`.gitattributes`（`*.java` / `*.js` / `*.yml` に `text eol=lf`）を消さない。** Windows の編集ツールが
  CRLF で書き戻すため、実質数行の変更が「全行変わった」差分になってレビュー不能になる。
- **`.project` / `.classpath` / `.settings/` は 2026-08-04 から ignore 対象。**
  post-check フックの compile が吐くので worktree ごとに生え、`wip-audit.ps1` の警告を埋めていた。
- **フォーク（`fork-handoff/arspaper/fork/`・`fork-handoff/elitemobs/elitemobs-fork/`）と `wiki/` は
  このリポジトリの対象外**（それぞれ独自の `.git`）。**`git clean` / `reset --hard` で消える。**
- **worktree を消す前に必ず中身を見る。** 未マージ commit が 0 でも**未コミット変更は消したら戻らない**
  （2026-08-04 の整理では `backups/orphan-worktrees-20260804/` へ patch 退避してから消した）。

配備・ビルド系:

- **`ops\launch\deploy.cmd --config` を使う。** ArsPaper の `materials.yml` は
  `MaterialConfigManager#load` が `saveResource(..., false)` しか呼ばないため、
  **jar 差し替えや `/ars reload` では無言で反映されない**。TF 側の `appendMissingKeys` に相当する機構が無い。
- **yml だけなら config-editor 経由でも配備できる**（保存が `deployPaths` へミラーする）。
  ただし**再シリアライズで本文コメントが消える**（K-3）。先に `docs/config-reference/` へ移設すること。
- **jar を差し替えたらフル再起動。** reload では不十分（`NoClassDefFoundError`）。
- **EliteMobs の配備は必ず全同梱 uberjar。** `*-min.jar` は `DungeonLocator NoClassDefFound` で起動不能。
- **`plugins/` に同名プラグインの jar を 2 つ置かない。** Paper は ERROR を吐くだけで起動を止めず、
  **どちらを読むかは不定**。配備のたびに `ls plugins/*.jar` で重複を確認する。
- **新しい config キーを足したら yml も一緒に配備する。** jar だけ入れると editor がその項目を空欄／OFF で
  表示し、**触っていないのに既定と違う値を書き込む**。
- **`combat/mob-profiles.yml` はリポジトリ側から上書きしない**（`importmobs` の生成物）。
- **Gradle が Java のバージョン番号だけを吐いて落ちたら JDK の取り違え。**
  `-Dorg.gradle.java.home="C:\Program Files\Java\jdk-21"` を渡す。Java が更新されるたび再発する。
- **`TrinityForge/.gradle/`（約 95MB）と `build/` は再生成物。** 消してもよいが、
  **並行セッションがビルド中だと巻き込む**（2026-08-04 の整理で 17:42 のビルドと重なった）。
- PowerShell 5.1 は `&&` を解釈しない。`ops/` 配下の `.cmd` は ASCII のみ
  （非 ASCII を書くと cmd.exe が行の途中から実行を始める）。

設計・実装系:

- **モブの敵対判定は Paper の `Enemy` マーカーが正。** Bukkit の `Monster`/`Animals` は使えない（HOGLIN が `Animals` 判定なのに敵対）。
- **ワールドゲートは `SkillExpConfig.worldExpRate(boolean)` の 1 箇所に閉じている。** 新しい EXP 経路は必ずここを通す。
- **採取ツールの判定に `stats.UseSkillDefaults` を流用しない**（`_AXE` を `HEAVY_WEAPONS` に落とすので素の斧で伐採できなくなる）。採取用は `gathering.GatheringToolMatcher`。
- **PvP のバランスは倍率だけで取らない。** 「1 発で最大体力の何 %」というスケールフリーな上限を併用する。
- **排他グループ（`group:`）は「同じ親を持つ兄弟」にしか効かない。** 路線固定で親を分けると no-op になる。`group` は A 帯だけに置く。
- **1.21.11 に弓の引き絞り時間を変えるレバーは無い。** クロスボウの装填だけ `QUICK_CHARGE` で縮められる。
- **採掘速度を `MINING_EFFICIENCY` / `BLOCK_BREAK_SPEED` 属性で実装しない**（Geyser 未対応＝統合版でゴーストブロック）。効率強化エンチャントのレベル操作が唯一の互換手段。
- **ソース系の倍率を共通経路（`Sourcelink#addToBuffer`）で掛けない。** 返却経路も通るので無限増殖する。
- **同じ式が複数箇所に散っていたら 1 メソッドへ集約する。** SP 付与式は付与・管理コマンド・reload 再計算の
  3 箇所にあり、片方だけ設定を見ると「レベルアップで増えた点が reload で消える」食い違いになる。

---

## 6. 参照先（一次情報）

| 知りたいこと | 見る場所 |
|---|---|
| **触る前に知らないと黙って壊す知識** | `docs/agent-context/README.md`（索引）。どのドメインでも `common-traps.md`、コマンドを打つ前に `ops-build-deploy.md` |
| config の各キーの意味・適用点 | `docs/config-reference/` 以下（yml 本文コメントは editor 保存で消えるのでこちらが正） |
| 仕様 | `docs/` 直下の `*_SPEC.md`。決定台帳は `docs/OPEN_DECISIONS.md`（LD-* はコードから参照されている） |
| 進行中のコンテンツ拡張 | `docs/design/2026-08-01-content-expansion-spec.md`（`-plan.md` が計画側） |
| **失効した設計文書** | `docs/archive/`（**正典ではない**。理由は同ディレクトリの `README.md`） |
| 過去の作業記録・解決根拠 | `reports/ACTIVE_RECORD_ARCHIVE.md`（**残タスクの一次情報ではない**） |
| 並列作業・worktree の作法 | `docs/agent-context/parallel-worktrees.md`。中断から復帰したら `ops\scripts\wip-audit.ps1` |
| サーバ運用・資源サーバ分離 | `ops/RUNBOOK.md`（周辺は `ops/README.md` から辿る） |

---

## 7. 作業履歴（新しいものを上に追記）

**全文は `reports/ACTIVE_RECORD_ARCHIVE.md` の §B。** ここは直近だけを 1 行で残す索引。

| 2026-09-04 | **予告機構の次段階（機構7〜10）と Codex 残指摘4件を実装**。①`fixed_zone`（床固定の持続領域。`cast-seconds` で展開までを予告し展開時に予算解放。術者死亡後も残る。`aura` は追従型で別物）②中断 `interruptible`/`interrupt-damage-fraction`(0.03)/`interrupt-lockout-seconds`(8)（詠唱中に最大HP比のダメージ or スタンで `MobAbilityInterrupts` 経由で中断→「詠唱中断」通知・同技ロック。**火力技は中断不可のまま**）③空振り硬直 `whiff-stagger-seconds`（誰にも当たらなければ移動停止＋「体勢を崩した」。8秒に1回。雑魚に付けない規約は yml コメント）④行動順 `ability-sequence`/`ability-interval-seconds`（`mob-overrides.yml` per-mob。順番指定は発動率を無視・空振りで次へ飛ばさない。ランダム経路は直近3回の行動語で重み付け）⑤引き寄せ/吹き飛ばしは被害者ごとに視線判定 ⑥`telegraph-lethal-atomic`（致命は脅威圏の全員分の枠が取れないと撃たない）⑦`telegraph-bar-style: block|ascii` ⑧机上検査の純関数 `TelegraphFeasibility`（設計正本の式を D95 込みへ置換）。設計正本・実装仕様（機構7〜10節）・レビュー反映表・エディタ（型/範囲/フォーム/per-mob 欄/テスト13件）を同時更新。⚠ 出荷 yml の値は未変更（挙動不変）。TF 5026/失敗37/skip4（失敗集合は着手前と同一）、エディタ 1480/失敗46（同一集合）。**TF jar の再ビルド・配備が要る** |
| 2026-09-04 | **ダンジョン改修の予告機構・必須6つを実装**（`2ad3e85`。設計正本 `docs/design/2026-08-31-dungeon-concept-rework.md`／実装仕様 `docs/design/2026-09-02-telegraph-mechanics-spec.md`）。①範囲判定を箱→水平距離の円（`AbilityShapes`。**判定と描画が同じ関数から座標を得る**。`vertical-radius` 既定3.0を新設）②`cast-seconds`/`lethal` と1tickの詠唱ループ（幾何は開始時スナップショットで固定）③予告レンダラ（円は輪郭だけ・直線は±太さの2レール・床印は中心点滅。予告フレーム無音／開始音1回／着弾5tick前に合図音）④`ActionBarRouter`（予告＞通知＞警告＞EXP＞AFK。予告中のEXP表示は捨てる。`[行動語] 技名 ▮▮▮▯▯ 残秒`）⑤`TelegraphBudget`（プレイヤー単位・致命1/合計2。抽選で予約不可の技は候補から外し**振り替えない**）⑥予告付き技は確率回避を通さない（`SymmetricCombatService` に `telegraphed` オーバーロード。既存経路は無変更）。⚠ **出荷テンプレートに `cast-seconds` はまだ書いていない**（雑魚396体の挙動は不変。数値を動かすのは闇の大聖堂の割当表を作る段階）。ただし **delayed_zone は `duration-seconds` を予告として読む**ので、輪の描画・予算・回避非適用・円判定は**今の出荷 yml でも効く**。**Codex による UX クロスレビュー**を並走させ 13 件の指摘のうち 9 件を実装へ取り込み（型別の不発条件／スナップショット／2レール／音の3段分離／行動語／受信者選定／終端処理の一元化 等）、4 件を設計側の残課題として `docs/design/2026-09-04-telegraph-ux-review-codex.md` に反映表付きで残した。⚠ **エディタの型一覧に `projectile_rain` が無く、出荷 `mob-abilities.yml` は検証エラーで保存できない状態だった**（2026-08-17 の Java 追加時の追随漏れ）→ 追加。⚠ **worktree ではフォーク不在の `NoSuchFileException` が失敗に混ざる**（`LegacyValhallaRuntimeContentTest`/`SpellBreakMarkerDriftTest`）。TF 4969/失敗37/skip4（失敗は全て他セッション未コミット yml 由来＋フォーク不在）、combat 系 605/失敗3（着手前から赤い武器DPS帯）、エディタ 1467/失敗46（着手前と同一集合）。**TF jar の再ビルド・配備が要る**。 |
| 2026-09-04 | **個別ランキングにプレステージが反映されず、プレステージ直後は順位表から行ごと消えていた。** `RankingMirrorStore#topSkillLevel/topTotalSkillLevel` が `s.level` だけで並べ、`level > 0` で絞っていた。プレステージはレベルと累計 EXP を 0 へ戻して段だけを上げる仕様なので、最も育っているプレイヤーが最下位へ落ちるどころか表から消える。値を実効レベル（上限レベル×段+現在レベル）へ変え、同値は段の多い方を上にした。合計（スキル合計）も同じ式で足す。`RankingMirrorStoreTopTest` に 4 件（**RED 証明済み**）。**外部の順位表プラグイン側の変更は不要**（値は TF の `rankingTop` から来る）。**TF jar の再ビルド・配備が要る** |
| 2026-09-02 | **品質+15なのに儀式エンドラ一式が劣悪。** エンドラは儀式なので `ritual_quality_bonus` だけを読む。鍛冶/Ars鍛冶の品質表示が作業台・儀式・品質運を混同しており、鍛冶の「全ての品質+3」は実値 `workbench_quality_bonus:+2` とも不一致だった。表示を実効値と経路へ統一し、両プレステージの適用先を固定する回帰テストを追加。品質昇華の結晶自体は対象装備の品質を必ず+1するだけ |
| 2026-09-02 | **`/tf status`の最大マナ/マナ効率表示、敵ドロップ品質、ガチャ鍵、品質昇華、儀式品質の6件を修正。** Ars最終マナ値を表示へ投影し、敵ドロップは死亡時にモブLv/専用`mob_drop_quality`で刻印（拾得者の開運を参照しない）。固定鍵はroll/不可解情報を持たない同一identityで生成。通常装備の品質昇華は品質だけを上げてpt/ロールを保持し、スレッドは専用lore更新へ委譲。儀式成果物の`quality-mode-offset`（+15−8=7）を回帰テストで固定
| 2026-08-29 | **減衰リセットの二重 clear／良薬切り下げのゾンビ行／既存スレッド魂縛の join 走査。** `/tf decay reset` の非同期は `wipeStored`（DB だけ）。`capToSnapshots` は期限切れを消す。Ars `ThreadSoulbindListener` が join+40tick・スロット変化・エンダー。フォーク側は TF commit に含まれない |
| 2026-08-29 | **拘束の空飛び／革チェスト解体6。** snare の JUMP_BOOST 128 は 1.21 で打ち上げ。属性で jump/move を 0 に（フォーク、commit しない）。革6は unique-key＋カタログ混入で素材数5。shape を歩き `minecraft:` だけ数える |
| 2026-08-29 | **解体24／一括葉ドロップ既定32／カタログ魂縛lore／魂縛解きの符を PAPER。** 金属チェスト24は防具装飾の鍛冶を素材数に足していた。`chain-drop-rolls-max` を editor 化して既定32。`/tf catalog` の Ars 先取りは catalog の bind/owner を焼き、join で所有者loreを足す。符の素材はリードとして使える LEAD から PAPER。TF 特殊アイテムタブで material/表示名/aura/lore を編集可 |
| 2026-08-29 | **editor: ポーション品質換算 GUI を外した／タブ切替の偽未保存。** 係数は本体が `alchemy-quality.yml` を直接読む。切替は GET 待ちに古い editor が残ると「次タブの id × 前画面の getData」を比較していた。navToken 再確認と保存後 `syncBaseFromEditor` |
| 2026-08-29 | **魂縛解きの符がカタログ補助に出る。** LEAD 推論が other。`TF_SPECIAL_ITEM_IDS` に加え、カタログ/ステータスの一覧からも除外。実コード確認済み（`split-views.js` clone・`forms.js` idsInCategory の2重除外が動作）。`罠3` テストの失敗は `chanter_ring`/`woodsman_whetstone`（新規サブウェポン draft）が `SUPPORT_TAB_ALLOWLIST` 未登録だった別件で修正済み。エディタで依然表示される場合は**サーバ再起動が必要**（古いプロセスが旧コードを保持）|
| 2026-08-29 | **W-304 エンチャント試練は Lv100 固定のまま技だけ弱めた。** `ability-damage-scale: 0.70`（10本）。試練 2/5/6/10 の振れ型を外し、中央値の通常打が同じになるよう `attack-power` を戻した。共有技テンプレは触っていない |
| 2026-08-29 | **W-302 魂縛解きの符。W-303 革防具のトリム／染色／脱色。** 所有者は TRADEABLE にして永続解除。装飾は identity を clone して色・trim だけ写す |
| 2026-08-29 | **W-300 厳選の護符が護符自身を選べる。W-301 エンチャント運が低いうちはバニラより弱い。** 券は random 層がある品だけ。運10未満はレベル-1（下限1）、以上は従来の格上げ（×2%、運50で100%） |
| 2026-08-29 | **W-299 称号がネームタグと中心・高さがずれる。** 本人非表示後の先読みを外し、スニーク等のオフセット変化 tick は補間を切る |
| 2026-08-29 | **W-57 スレッドが `/tf status` に乗らない。W-298 重装備 −5% 移動・回避削除・防御 +10%。** スレッドは装備 PDC をライブ合算（addon 優先）。重装 63 部位を一律化。Wiki 03 |
| 2026-08-29 | **W-295 金床の結果空＋スレッド lore。** 表示39は次tick（空なら結果を戻してから）。enum のマナ数値は 0、yml に無い値は乗らない。セットは `Nセット`＋全角インデント、フレーバーは1行紫。持ち替えで組み直す |
| 2026-08-29 | **W-294 装備破壊で装着スレッド消失。** 実体は装備 PDC のみ。Ars は `PlayerItemBreakEvent` で GUI 取り外しと同じ組み直し。TF の手書き耐久破壊も同イベントを撃つ |
| 2026-08-29 | **総合スキルツリーで無関係な列が格子状につながる。** W-250 の列ずれ 0 でも残る。主軸 B が A の枝と同じ行に居るため、B の扇が足元/天恵の縦の続きと同じ通路セルを共有し `merge` が十字になる。BFS が北優先でノード行の隙間へ落ち、兄弟の間に L 字も残る。→ 別親の縦×横は縦を残す／ノードの東西1セルは経路に使わない。座標 yml は無いので **jar 再ビルド・再配備だけ**（config 不要） |
| 2026-08-29 | **W-293 醸造の見習い後にバニラの効果反転が止まる。** 品質で WATER へ倒したビンにクモの目を入れると、バニラは水→弱化しか見ず W-116 が醸造ごと止めていた。倒す前の種類が反転表にあれば書き戻す。解放式カスタムは反転しない |
| 2026-08-29 | **W-289 宝箱スレッド10種の item-stats 表示タブ／W-290 詠唱効率で消費マナ1／W-291 圧縮フグ・熱帯魚・ハチミツ729倍／W-292 バインド対象をオフハンド固定。** ステ定義は `STRING#300070-079` のままピンと戦闘系ネストへ。軽減は分数と percent-point を同じ15%に揃え、enum 既定10%を消した。圧縮は materials `_1x/_2x/_3x`（9倍だけ edible）。バインドはオフハンド占有時にホットバーへ落ちない |
| 2026-08-29 | **スレッド魂縛を「作れるか」で分けた。** catalog に作業台/儀式レシピがある種は TRADEABLE、レシピ無し（宝箱・ドロップ・ガチャ等）は SOULBOUND。匠・採掘などは catalog にレシピが無いので SOULBOUND |
| 2026-08-29 | **GitHub Wiki を公開。** `Klee319/trinityforge.wiki.git` master へ、幸運(採掘)の再設置砕き・ドロップ増加・バックパック・厳選の護符。事典の catalog 由来ページは未コミット yml なので出していない |
| 2026-08-29 | **Wiki をコードに追従。** 幸運(採掘)のシルク／再設置砕き、ドロップ増加の stat-caps、バックパック max4・ページ送り、厳選の護符。生成器のスレッド重ね表記も `max` 既定1へ |
| 2026-08-29 | **幸運(採掘)は鉱石の再設置砕きに乗せる。GTH-01 はグロウストーン等の循環だけ。** 原石クローンは意図（バニラ幸運と別枠）。シルク装備中は従来どおり乗らない。残骸はドロップがブロック自身なので設置済みは弾く |
| 2026-08-29 | **バックパック消失／部位選択／総枠cap／ドロップ増加の+200%撤去。** ダブルクリック吸引とロック枠の余りは持ち主へ返す。`/ars backpack` は複数部位なら選択。`max-inventory-slots` を slots で床上げしない。`max: 4` で 108 到達。`MobDropRoller.clampBonus` は stat-caps 任せ |
| 2026-08-29 | **カタログで素材・スレッドを取る／厳選の護符がロール運を見る／バックパック複数枠。** `/tf catalog` は Ars 実体を先に配り、カタログ外は素材/スレッドタブへ。護符は `CraftRollMods`。バックパックは `slots`×本数を `max-inventory-slots` で打ち、54超は最下段ページ送り |
| 2026-08-29 | **既存スレッドの表追従（品質・pt維持）／称号を Java でも本人非表示／連鎖破壊で残る額縁の ERROR。** スレッドは `refreshLoreKeepingIdentity`（`restampWithQuality` は seed 再発番なので使わない）。称号は `visibleByDefault=false` + teleport 後も hide。`setType(AIR)` の前に hanging を外す |
| 2026-08-29 | **スレッド魂縛を catalog `bind-type` へ／バックパック・浮遊の主軸を常用向けへ／セット閾値1を固定ステへ吸収／ボス最終段階34体に戦闘スレッドドロップ。** 魂縛はエディタの SOULBOUND。宝箱10＋敵ドロップ。CMD 帯は TF 未ロード時だけ。新戦闘種は作らず既存戦闘24の未使用2種（猛攻・堅陣）を含む割り当て。強ダンジョンほど強い既存種 |
| 2026-08-29 | **W-283 EXP解呪の良薬を減衰量削減＋極の2時間無効化へ差し替え。W-284 `/tf catalog` にカタログ外カスタム品。** 並は 70% 天井（離散段では 65.6%）。運営は `/tf decay reset` / `immune`。無効化は `daily_exp_immune` へ永続化。無効化中は蓄積を増やさない |
| 2026-08-29 | **W-282 エンチャントオーラがテーブル／金床を塞ぎ、砥石後にエンチャント表示が消える**。真因はダミー耐久力I+`HIDE_ENCHANTS`。`setEnchantmentGlintOverride` へ移行。Ars は 08-14 に同じ穴を踏んで直済み |
| 2026-08-29 | **W-281 儀式アップグレードでエンチャント等がはがれる**。品質／ロールは実行者で新規ロールのまま、コアのエンチャント・スレッド・バックパック・個体 PDC を `ItemUpgradeCarryOver` で写す。接頭辞 `mage_` 限定を外した |
| 2026-08-29 | **`deploy.cmd` は EliteMobs の成果物が実機と違うときコピーする。** ビルド SKIP（ソースが jar より古くても手ビルド済み）でも SHA256 が違えば main / dev へ配る。同じ中身は触らない。配置先 `launch\` は `deploy-launch.cmd` で配り直す |
| 2026-08-29 | **W-309 EM 戦闘の `String.replace` NPE。** W-278 が CustomName を消した直後、`setLivingEntity` が null を `EliteEntity#name` へコピーしていた。スクリプト `$bossName` と死亡メッセージ `$entity` が落ちる。`retainStoredName`。**EliteMobs uberjar の再配備が要る** |
| 2026-08-29 | **W-278 再発: 視線合わせのバニラ名札。** 実機 jar には MEG/FMM 生成修正が入っていたが、Main/Dev に FMM/MEG が無い。`customNameVisible=false` でも CustomName は視線合わせで出る。抑止中は `applyLivingNametag` が文字列も消す。**サーバ停止後に EliteMobs uberjar を差し替え** |
| 2026-08-29 | **W-275 想定内クローズ / W-278 EM名札 / W-279 儀式 result-amount / W-280 スレッドが補助タブ**。うさぎは非ダイナミックなので推奨20で死ぬのは仕様。EM は MEG 生成の `setName(..., true)` と FMM の visible 無視。editor は儀式個数を `result-amount` に接続し、STRING スレッドを補助へ落とさない |
| 2026-08-29 | **実サーバ報告バッチ（W-260〜W-277）**。楔はメインハンド＋クリックで確認GUI。スレッドは max stack 1＋ホッパー刻印。うさぎは推奨Lv20 で全装備1発死（試練の攻撃力絶対値）。数値は未変更 |
| 2026-08-29 | **実サーバ報告バッチを §3-E に 1 件 1 行で登録**（バグ14 + 非バグ5。W-260〜W-277。ソースベリーは W-258 の再報告）。着手前の台帳 |
| 2026-08-25 | **スキルツリーの配置（W-250。付呪と総合が「直っていない」の2回目の報告）**。⚠ **真因はレーンを配る順序**で、コネクタの描画側ではない。**主軸ノードは最初に全部トランクへ置かれる**ため、深さ順のウェーブでは**どの主軸の扇も下から来たチェーンの続きより先にレーンを取る**。押し出された続きは 2〜4 列横へずれ、その斜めのコネクタが扇の線と同じ行の隣のセルに並ぶ ── **セルの境目が無い GUI では別々の線が1本の長い横線に見える**（これが「つながり方がおかしい」の見え方。同じセルの重なりではない）。→ 行を下から閉じ、同じ行では続きを先に置く。併せて**「1段上へ逃げる」コストを横4レーンぶんに引き上げ**（行＝レベル帯なので、帯を飛ぶ方が横へ伸びるより悪い）。⚠ **W-218 は「空きセルの探索順」の修正で、配る順序には触れていなかった**ので効かなかった ── 順序が先に決まる以上、探索側では直せない。⚠ **測る指標を間違えると「直った」が証明できない**: 既存の帯ずれ／線の重なりでは POWER が old==new に見えた（帯ずれは y だけ・重なりは同一セルだけを見るため）。**列ずれ**（続きが親の列から外れた数）と**融合**（別々のコネクタが同じ行の隣のセルを横に走る数）を足して初めて捕まった → 16ツリー実測 **列ずれ 24→0 / 融合 18→0 / 帯ずれ 1→0**、接触・迂回・主軸侵入は悪化なし。恒久ガードは既定の配置そのものを縛る形で `AllSkillTreesLayoutIntegrityTest` に追加し、**既定を旧順序へ戻すと 12 ツリーで落ちる**ことを確認（付呪・総合が名前で出る）。⚠ **座標は yml に無いので jar の再ビルドと再配備だけで直る**（config 配備は不要）。TF **4752/失敗26/skip2**（失敗26は着手前と同一・全部未コミット yml 由来） |
| 2026-08-25 | **追従の遅れと粒子の量（W-247/W-248）、軌跡の形状（W-249）、エディタが保存を拒否する件（W-246）**。⚠ **W-246 の教訓**: 「yml に書いた値を editor が拒否する」は**リポジトリではなくプロセスの鮮度**。Node のモジュールキャッシュのせいで `lib/schema.js` の変更は**再起動まで一切効かない**（既知の「入力欄が空欄」と同じ原因の別症状）。テストは緑・ファイルも正しい・それでも実機は古い、という食い違いが起きるので、**エラー文の選択肢とリポジトリの配列を突き合わせる**のが最短の切り分け。⚠ **W-247/248 の真因**: 追従の遅れは補間長ではなく**目標地点の古さ**。`player.getLocation()` はクライアントが送ってきた位置で、**自分の本体はクライアントが予測して即座に描く**（ネームタグは本体そのもの）ので、サーバ位置に置いたものは構造的に後ろへズレる。→ `MotionLead`（前回 sample からの移動量で1tick先読み。`getVelocity()` はクライアント操作のプレイヤーでは当てにならないので使わない／上限超えの差分は縮めずに捨てる／sample は毎tick 1回だけ）。⚠ **粒子の「量」は count ではなく count × 見ている人数**（座標が1点ずつ違う形状は点の数だけ `spawnParticle` を呼び、per-viewer 送信なので人数が掛かる）→ 上限 400→64、出荷値も一段下げ。回帰は**先読みを外す／進行方向を使わない／軌跡を前方へ向ける／語彙から trail を抜く**の4通りの差し戻しで実際に落ちることを確認（Java 4本・editor 2本）。TF **4750/失敗26/skip2**（失敗26は着手前と同一の未コミット yml 由来）、editor **1443/失敗28**（schema-rewards は 39/0。byte 同一性の失敗は `progression/achievements.yml` で自分の変更ではない） |
| 2026-08-25 | **パーティクル演出の全面拡張（W-242〜244）と、右クリック干渉の網羅監査（W-245）**。⚠ **W-244 の非自明な真因**: 形状が2種だったことより、`spawnParticle(particle, loc, count, ox, oy, oz, extra)` は **`count == 0` のときだけ `ox/oy/oz` を「飛ばす向き」・`extra` を「その速さ」として読む**（`count > 0` だと `ox/oy/oz` はばらつきの箱）。旧実装は全形状で `count>0` かつ `extra` を 0 に焼いていたので、**「円形に拡散する」は原理的に書けなかった**。→ 形状 → 呼び出し列（`ParticleGeometry.Emit`）の純関数へ切り出し、`speed>0` のときだけ方向指定モードへ落とす。形状は8種（aura/circle/sphere/burst/helix/pillar/arc/point）で、**形状ごとに読むキーが違い、書かなかったキーはその形状の既定値で埋まる**。**W-242**: `burstAt` が常に `origin.getLocation()` を使っていたので、リスナーは当たった座標を持っているのに捨てていた（遠くを掘っても足元から出る）。→ ブロック中心／相手の胴／矢の着弾点へ。**飛び道具は着弾時の手を見ない** ── 飛んでいる間に持ち替えられるので、放った瞬間に矢の PDC へ刻印を移す。魔法（Ars 触媒）は TF 側にイベントが無く未対応。**W-245**: `PlayerInteractEvent` を見る17クラスを監査し、**農業EXPだけが「意図」で配っていた** ── バニラは**スニーク中で手が空でないならブロック操作を丸ごと飛ばす**ので、**熟したベリー／満タンの巣をスニーク連打すると実が減らないままEXPが無限に入った**（`PlayerInteractEvent` はキャンセルされないので `ignoreCancelled` では防げない。W-210 の盾と同じ形で、引き金がスニークな別経路）。→ ベリー／ツタは `PlayerHarvestBlockEvent`、蜜は**次tickに蜜量を読み直して減っていたときだけ**配る。回帰は**配線を戻すと実際に落ちることを実走確認**（発生位置3本／農業EXP3本）。TF **4733/失敗26/skip2**（失敗26は**着手前と完全に同一の26件**＝ユーザーの未コミット yml 由来。`skills/base/*_armor_progression.yml` の `exp_damage_piece` 10→30・`farming_progression.yml` の作物 19.2→30 などがテストの固定値と食い違っている）。設定エディタ **1438/失敗30**（うち schema-rewards は 37/0。失敗30も未コミット yml 由来で `progression/achievements.yml` の開いて保存の同一性ほか） |
| 2026-08-25 | **「防具値1点あたりEXP倍率」の正体（W-241）**。`skills/base/{light,heavy}_armor_progression.yml` の `exp_multiplier_point` で、被弾EXPに掛かる「装備の硬さ」係数（式は `EXP ×(1 + 防御率の合計 × この値)`）。⚠ **2026-08-15 の防具値廃止で入力の桁が約 1/67 になったのに、出荷 yml の値が旧スケール（0.05）のまま残っていた** ── コード既定だけ 0.05→3.33 へ直したが、**yml があればコード既定は読まれない**。結果、係数は `1+20×0.05=2.0` から `1+0.30×0.05=1.015` へ落ちていて**硬さボーナスが実質消えている**。数値はユーザーが設定エディタで調整中（未コミットで 0.07）なので**エージェントは触らず**、設定エディタのラベルだけ「装備の硬さEXP係数」に訂正して桁の変化を説明文に入れた。最良装備で従来どおり約2倍にしたいなら 3.3 前後 |
| 2026-08-25 | **ソースリンクの転送が「まだ100ずつ」だったのを、網の側にも割合排出を入れて解消した**（報告「ソースリンクの転送速度がまだ100ずつらしい」、ArsPaper fork `c71565b`）。⚠️ **2026-08-24 に入れた割合排出は隣接供給の経路にしか効いていなかった** ── ソースの転送は2系統あり（ソースリンクの隣にジャーを置く `Sourcelink#supplyAdjacent` と、ドミニオンワンドで結んだ網 `SourceNetwork#tickTransfer`）、`transfer.sourcelink.drain-ratio` は前者だけを見る。後者は `transfer.network.max-per-transfer` の**定額100 ÷ 40tick ＝毎秒2.5点**のままで、**網には階梯倍率もコア倍率も掛からない**ので上位リンクにしても上位ジャーにしても永久にこの速度だった（ソース機関1個=3,000万を運ぶのに約7日）。⚠️ **今回の配備でこの上限が初めて表に出た** ── 同じ配備に入った W-104（網の送信元がソースリンクだと残量を常に0と誤判定して一度も転送しなかった）の修正で、それまで死んでいた経路が動き始めた。→ 1リンクの上限を `SourceDrainPolicy` で引き直し、隣接供給とまったく同じ「定額と残量×割合の大きい方」にした。**上限は毎リンク引き直す**ので、リンクが複数ある送信元では残量が減るほど1本あたりの上限も下がる（定額時代の「1リンクにつき上限1つ」の意味を保つ）。キーは `transfer.network.drain-ratio`（既定 0.25 = 隣接と同値）を新設。周期が違う（隣接100tick / 網40tick）ので同じ割合でも網が2.5倍速く、後から別々に絞れる必要があるため別キーにした。⚠️ **未定義キーは既定値で埋まるので config 配備は不要**（ArsPaper の yml は `saveResource(..., false)` で配備先へ届かないため、これは意図した設計）。回帰3本（既定が隣接と同値／yml 上書きとクランプ／`SourceNetwork` が定額を直接使わない配線ガード）で、**配線を戻すとガードが実際に落ちることを実走確認**。あわせて別セッションが未追跡のまま残していた W-104 の一式（`SourceStorage` と貯蔵種別の判定、テスト2本。既に配備済みの jar に入っている）を確定した。ArsPaper fork **575/失敗0/skip0** |
| 2026-08-25 | **判断確定ぶんの実装 第2波（入手経路ゼロの4件 / ポーション4件 / スキル画面と印の並び / editor の巻き戻し / 軽装の耐久）**。⚠️ **入手経路ゼロだったインフィニティの道具4種に作業台レシピを付けた**（`4e5705e`）── 素材は同じ帯のインフィニティ武器と同じ核・紫水晶・ブリーズロッドで、形はバニラの各道具の並びを踏襲。**レシピは書いただけでは成立しない**ので、素材IDの解決・形の妥当性・**他レシピとの形の重複**（同じ並び・同じ素材だと先に登録されたほうだけが成立し、後は永久に作れない）の3点を `ShippedInfinityToolCraftabilityTest` で固定した。⚠️ **editor 側に「レシピを再追加しない」という逆向きのガードが残っていた**（`catalog-combat-content.test.js`。入手経路が未確定だった当時の名残）ので向きを反転。⚠️ **ポーション4件**（`6ede2e3` / `61473da` / `b552cf9` / `3374afe`）── ①統合版で水入り瓶に見えるのは base を WATER へ倒すのに `setColor` を焼いていないため。バニラの「持続時間で重み付けした平均色」を再現して焼く。②統合パークが一度も発火しないのは**ポーションのスタック上限が1**なので旧条件（`PLACE_*` かつ両スロットにポーション）が構造的に成立しないから。実際に飛ぶのは `SWAP_WITH_CURSOR`。③カスタム効果ポーションの醸造そのものを禁止（延長/強化/スプラッシュ化/残留化の4救済を先に評価してから）。④行き止まり素材のテストは実データと食い違っていた（実在しないIDを前提にしていた）ので現仕様を固定する向きへ書き直し。⚠️ **「通知の瞬間しか残り時間が見えない」の実体は配線漏れ**（`7c8992a`）── 残り時間を出す部品は 2026-08-18 からあったのに、配線先が最下段の選択バーだけで一覧モードには繋がっていなかった。さらに「次の段階まで」を返す関数が**最初から存在するのに一度も呼ばれておらず**、段階的に戻る設計なのに途中経過が出ていなかった。ダンジョンの印は実績側も図鑑側も命名順のままだったので、モブ上書き設定のコメントから実読した難易度の昇順へ並べ替え（テストは期待順序を書き写さず難易度表を実データから組み立てる）。⚠️ **editor から出荷 yml が無言で巻き戻る経路を2つ塞いだ**（`7332fd7`）── ①生成スクリプトが `--force` で出荷 `item-stats.yml` を丸ごと上書きしていた（生成側の表は手調整値も孤児CMDも持たないので上書きは必ず調整を落とす）ので、出荷ymlを読むこと自体をやめてプレビュー出力に変更。②保存時の楽観ロックが**「未作成として読み込んだ config」だけ照合を丸ごと飛ばしており**、開いている間にエージェントが新規作成した内容が無条件で上書きされていた。⚠️ **軽装が脆いという前提は既に解消済みだった**（`7b2b795`）── 台帳の「軽装 HP 30.3」は防具の住み分け作業の**着手前**の実測で、同じ日の同じ作業で解決済み。出荷設定を実際に読んで束縛者第4段階の通常攻撃を本番と同じ計算に通すと軽装は重装の約1.19倍耐えるので、yml は1行も変えず、比が1.00を割ったら落ちるテストだけを置いた。⚠️ **`catalog-combat-content.test.js` に残る赤2件（`wooden_axe_tool` 系）は実データに追随していないテスト側の腐り** ── カタログに `wooden_axe_tool` というIDは1つも存在しない（別セッションの担当領域なので手を付けていない） |
| 2026-08-25 | **判断確定ぶんの実装 第1波（炸裂半径 / 台帳の腐り 5 件 / 死にソース削除 / 戦闘レベルの既定ずれ / 鍵の説明文）**。⚠️ **同じ「既定値を書く場所が2つある」バグを2件見つけて両方潰した** ── ①炸裂（ArsPaper fork `b037594`）: 詠唱側と投射着弾側が同じ2キーを別々のリテラル既定値（どちらも 2.0/1.5）で読み、出荷 `glyphs.yml` は 1.0/0.5。`glyphs.yml` は `saveResource(..., false)` なので**配備済みサーバの yml は更新されず、キーが無い環境では 2.0 がそのまま実挙動**になっていた。`BurstForm.resolveBurstRadius()` 1本へ寄せ、既定値を出荷値へ。②戦闘レベル（`bbec466`）: `CombatLevelConfig.DEFAULT_PILLARS` が **top1/1.0** のまま（出荷 yml は 1.5）で、`pillars:` を持たない config を読んだ環境だけ純特化 100 のプレイヤーが戦闘レベル 100 になる。`combat/damage.yml` の level-cutoff は **67** を前提に書かれているのでカットオフが丸ごとずれる。既定を出荷値へ揃え、「出荷 yml の pillars ＝ pillars: 欠落時の既定」と「純特化 100 → 67」をテストで固定。`combat-level.yml` 冒頭の worked example も除数 1.0 時代のままだったので実値へ書き直した。⚠️ **許可リスト方式の検査が腐って無効化していた** ── `CatalogVanillaOperationPolicyConfigTest.PENDING_CMD_ASSIGNMENT` の3チケットは 2026-08-09 に採番済みなのに例外に残り、その3件だけ「素材+CMD復元ガード」の検査から外れていた（`e7c8e04` で撤去）。同テストが**出荷カタログの中身に寄りかかっていた**2箇所（輪転の素材固定と「ブロック素材の分岐を通る」検査）も、素材が `GLOWSTONE`→`GLOWSTONE_DUST` へ変わった時点で偶然赤くなっていたので、ブロック分岐は専用フィクスチャへ移した。⚠️ **「印5個」は表示だけが旧仕様だった**（`315753a`）── 世界を繋ぐ者の鍵のレシピは印を1つも要求しない固有素材5種の形（意図した変更）。説明文と、旧主張を固定していたテストの両方を現仕様へ。**副作用として印28種の消費先がゼロになった**ので D-21 へ起票。⚠️ **入手経路ゼロの再計測結果は「非 draft で4件」** ── 出荷カタログ 457 件（準備中 80 件）を供給レイヤ全走査で数え直し、残るのは**インフィニティの道具4種（ピッケル/シャベル/斧/クワ）だけ**。88 件という旧数字は完全に stale。→ D-20 へ起票。赤いはずのガード5本（儀式素材の入手性3・ウォーデンの触手2）と儀式素材EXPの網羅・力IIIの醸造ペアはいずれも**実走して全緑**（力III は「作れるようになった」のではなく `STRENGTH` の醸造グループ自体が出荷 config から消えている → D-22 へ起票）。農業Dの説明文（EXPを配っていないのに「バニラEXPを追加で獲得」）も実態へ。Wiki の死にソース `04-アイテム図鑑.md` は削除（`0d55afd`、差し替え表と legacy 名の行は**消せない**＝他の原稿11箇所がまだ原稿名でリンクしており行き先が無いと生成が例外で止まる） |
| 2026-08-25 | **パーティクルシードを「特殊アイテム設定で作った専用アイテム」にできるようにした**（要望「seed は特殊アイテム設定みたいな感じでクラフトレシピや material/テクスチャを定義できるようにしたいね」）。⚠️ **専用の設定層は作っていない** ── 調べたところ `seed-item:` は最初から `custom:<カタログID>` を受け、金床の判定（`ParticleSeedMatcher`）も配布（`ParticleSeedDelivery`）も `CrossPluginItemResolver` 経由なので、**見た目・テクスチャ・クラフトレシピは `items/catalog.yml` のエントリ側にすでに全部書ける**。要望の3つを満たすのに足りていなかったのは配線1本だけで、ここで専用 config を足すと editor とカタログのどちらが正か決まらなくなる。⚠️ **足りていなかったのは `/tf settings` のアイコン** ── `SettingsGui#seedIconOf` は `Material.matchMaterial("custom:xxx")` が null になるので**カタログ品のシードを一律 `FIREWORK_STAR` に落としていた**。カタログでテクスチャを定義しても一覧には出ず、「金床へ持っていくのはどれか」が見た目から分からないままになる（エラーも警告も出ない）。→ `SettingsGui` に `custom:` を実体へ解決する種（本番は `CrossPluginItemResolver::create`）を**コンストラクタ引数で必須にして**渡し、実体の material と `custom-model-data` をそのままアイコンにする。lore の「素材:」もカタログ品の日本語表示名を出す。⚠️ **setter にしなかったのは、配線忘れが「カタログ品のシードだけ代用アイコンで出る」という実機の目視でしか気づけない劣化になるため**。⚠️ **借りるのは見た目だけ** ── カタログ品には説明文・属性・エンチャントが付いてくるので、閲覧専用ボタンでは `HIDE_ATTRIBUTES`/`HIDE_ENCHANTS`/`HIDE_ADDITIONAL_TOOLTIP` を立てて中身を隠す。実体の生成はカタログ側の任意コード（ステータス抽選）を通るので**例外を投げさせない**（壊れたエントリ1件で設定画面が開かなくなる）。回帰 `aCatalogBackedSeedShowsTheCatalogItemsOwnLook` が material と CMD を固定。出荷 yml の見出しコメントと editor の `seed-item` 欄に「専用アイテムにしたいときは特殊アイテム設定で作ってから `custom:<ID>` を選ぶ」を明記。**出荷の7件は Material 指定のまま**（カタログ化はテクスチャ資産と CMD 台帳の新番号が要るゲーム内容側の判断なので別途）。TF **4677/失敗30/skip2**、editor **失敗25** ── どちらも棚卸し時点と同一の他セッション未コミット yml 由来で、`SettingsGui*` / `ParticleSeed*` は全て緑 |
| 2026-08-25 | **耐久を持つ道具にスペルをバインドすると、その道具が詠唱ごとに磨耗していたのを触媒だけに限った**（報告「耐久値があるものに魔法をバインドすると魔法発動時に耐久値が消費される → 触媒のみ減るように」、ArsPaper fork `7c383c6`）。⚠️ **原因は `SpellCaster#consumeCastDurability` の門が「最大耐久0でなければ減らす」だけだったこと**。この機能(M-5)は「杖の素材を `BLAZE_ROD`(最大耐久0)から剣系へ移したので詠唱でも減らす」ために入れたものだが、`SpellBindListener#canBind` は **ArsPaper のカスタムアイテム以外なら何にでもバインドを許す**（触媒だけが例外的に可）。結果、ネザライトの剣・ツルハシ・防具にバインドした瞬間からそれ自身が詠唱ごとに減り、**バインドが実質デメリット**になっていた。⚠️ **判定は新規に書かず既存の定義へ寄せた** ── `use-skill: ARS_MAGIC` または `spellbooks.yml` の `catalysts:` 登録、つまり魔法のステ供給元(`MagicStatSourcePolicy`)とアチーブメントの `catalyst_cast` と**同じ門**（TFカタログの杖10本は `catalysts:` に載っていないので、`catalysts.yml` 登録だけで見ると杖が対象外になる ── 2026-08-23 に同じ穴でカウンタが永久に0だった）。⚠️ **`isCatalystCast(catalyst, castItem)` は流用できない** ── あれは「その詠唱が触媒由来か」で、どちらか一方が通れば true になるので「この1本を減らしてよいか」には使えない。名指し用に `TrinityForgeBridge#isCatalystItem` を追加した（TF 未ロードでも `catalysts.yml` 側は生きるので「TF が落ちている間だけ普通の剣が減る」ことは無い）。判定は `CastDurabilityPolicy#damageFor(boolean, int, double)` の第1引数として1か所で決める（このフォークはサーバ実装を持たないので、門の挙動はここでしか固定できない）。回帰は触媒でないアイテムはどの設定・どの乱数でも0／触媒は従来どおり減る／`SpellCaster` が門を `damageFor` へ渡している の3本で、**配線を外すと3本目が実際に落ちることを実走確認**。ArsPaper fork **570/失敗1**（失敗1は `ThreadRitualRecipeConfigTest`＝設定エディタ由来の未コミット `items.yml`（スレッド枠拡張の `max-slots`）で、本件と無関係 → K-40） |
| 2026-08-25 | **刻印を消すシードを既定で用意し、刻印中のシードを道具の説明文へ出すようにした**（要望「デフォルトでシード埋め込みを消すシードが欲しい」「埋め込んでいるシードを lore に注入（日本語で表記）」W-221）。①`particle-seeds.<id>` に **`display:`（日本語名）と `clears:`（刻印を消すシード）** を追加。②出荷に `seed_clear`（素材 `INK_SAC`・表示名「消去」）を1件追加し、既存6件に日本語名（焔／氷華／魂火／雷花／虚空／花吹雪）を付けた。⚠️ **消すシードは解放を要求しない** ── 剥がす操作は報酬ではないし、付け間違いを取り消せないほうが害が大きい。⚠️ **粒子を持たないので `particle` 必須の検査を通せない** ── ここを直さないと「config に書いたのに起動時の警告1行だけ残して消える」＝金床に何も出ない形で壊れる（`ShippedParticleSeedClearAndDisplayTest` が**実際のローダを通して**固定。yml を直接読む検査ではこの分岐を守れない）。③lore 行は `ParticleSeedLore` の1関数を**2箇所から呼ぶ** ── TF のカタログ品は `ItemAssembler#assemble` が lore を毎回ゼロから組み直す（持ち替え・装備変更・参加時）ので付与時に足しただけの行は次の組み直しで消え、逆にバニラの道具は組み直しの対象外なので組み直し側だけでは一度も出ない。⚠️ **`ItemAssembler` へは setter でなくコンストラクタ引数で `SpecialRewardsConfig` を渡した**（配線を忘れると「付けた直後だけ行が出て持ち替えると消える」という無言の壊れ方をするため）。⚠️ **古い行の除去は「粒子: 」の前置きで探す**（行の構造一致で書くと、`display` を変えた瞬間に古い行が永久に残る ── それが表示名を変える動機なのに）。回帰は付与／付け替え／消去／未刻印の道具には出さない／改名後の回収の5本。editor 側は `display`・`clears` の入力欄と、`clears` でないシードの `particle` 欠落を保存前に止める検査を追加。TF **4676/失敗30/skip2**（失敗30は棚卸し時点と同一の他セッション未コミット yml 由来）、editor **1429/失敗25**（同じくデータ系。報酬スキーマ 33 件は全緑） |
| 2026-08-25 | **パーティクルシードの付与を作業台から金床へ移し、持っているシードを `/tf settings` で見られるようにした**（報告「パーティクルシードを作業台で付与できない。金床形式にしたいね。また現状何のパーティクルシードがあるかをGUIで確認するすべがない」W-214/W-215）。⚠️ **作業台版が「たまたま動くかどうか」でしかなかった理由**: 旧実装は `PrepareItemCraftEvent` で結果枠を差し替えるだけで、素材の消費を「バニラのクラフト盤面が勝手にやる」ことに頼っていた。盤面に一致するレシピが無い以上そこはバニラにとってただの空の結果で、取り出しの成否は経路（通常クリック／シフト／統合版クライアントの逐次配置）ごとに変わる。さらに作業台には `CatalogWorkbenchListener` / `CatalogCraftGateListener` という「カタログ品が盤面に乗ったら結果を消す」ガードが**同じ `HIGH`** で並んでおり、どちらが先に走るかは**登録順に依存**していた（TF の武器・道具はほぼ全部カタログ品）。→ 金床（入力2枠・結果1枠が構造として固定）へ移し、`PrepareAnvilEvent`(HIGH) で結果を出し、**取り出しは `InventoryClickEvent` をキャンセルして消費・レベル支払い・受け渡しを全部自前でやる**。⚠️ **取り出しをバニラに任せてはいけない** ── バニラの金床は素材の消費数を自分で計算した `repairItemCountCost` で決めるので、プラグインが後から差し込んだ結果ではその値が 0 のままになり、**右スロットのスタックを丸ごと消す**（ブレイズパウダー64個で1回の付与、という事故になる）。回帰 `takingTheResultConsumesExactlyOneSeedItem` がここを固定。⚠️ **`CatalogVanillaOperationGuardListener`(HIGHEST) への除外追加が必須** ── 無いと HIGH で入れた結果を必ず後から消され、**バニラの道具にしか付けられない**（木材修繕が 2026-08-05 に踏んだのと同じ穴）。併せて①レベルコスト1（0にするとバニラの金床が「コスト0の結果は取り出せない」ため結果が見えているのにクリックしても何も起きない）②既に同じシードが刻印済みの道具は対象外（素材とレベルだけ消えるのを防ぐ）③解放していないシードは結果が出ない（2026-08-21 の保有ゲートを維持）。W-215 は `/tf settings` 最下段の閲覧専用一覧（45=金床アイコンの使い方見出し、46〜51=6件、52/53=ページ送り）。**解放済みは素材そのもののアイコンで出す** ── プレイヤーが探すのは ID ではなく「どのアイテムを金床へ持っていくか」だから。件数で黙って切らない（称号が1行8件で打ち切られ12件が永久に見えなかった 2026-08-17 の再発防止）。解放通知の文面も金床＋`/tf settings` の案内へ更新。TF **4667/失敗30/skip2** ── 失敗30は全て出荷 yml を読む data 系（他セッションの未コミット yml 由来）で、`ParticleSeed*` / `SettingsGui*` / `CatalogVanillaOperationGuardListenerTest` / `WoodRepair*` は全て緑 |
| 2026-08-25 | **魔法ツリーの氷風Ⅰが主軸の列に置かれて線が読めない**（W-218 の作業中に発見 → ユーザー回答「意図的でない」、`9379270`）。`skilltree/ars_magic.yml` の `B-1-1` が `role: intermediate` で、intermediate は主軸の列に置かれるため B(2,6) と C(2,4) の間 (2,5) へ割り込み、上下ともコネクタの隙間がゼロになっていた（`AllSkillTreesLayoutIntegrityTest` の「B -> C が他ノードを迂回」＋接触2件、`AllSkillTreesProgressionTest` の隣接4件の原因）。対になる `B-2-1` と同じ `role: branch` へ。**同ファイルに設定エディタ由来の親付け替え5件が未コミットで乗っていた**ので一緒に確定した |
| 2026-08-24 | **経験値瓶がスタック1のときだけ1クリックで格納と取出を両方やっていた**（報告「スタックが1の時正しく経験値を取り出し/保存できない。空瓶だと保存→取り出し、保存済の瓶だと取り出し→保存」W-220、`836b9b6`）。⚠️ **真因はクライアントが1クリックで2パケット送ることと、CraftBukkit の抑止条件がアイテム同一性で書かれていること**。ブロックに向けた右クリックでは `UseItemOn` の直後に `UseItem` が飛ぶ（バニラの瓶は `useOn` が PASS を返すのでクライアントが「ブロックへの用途は無かった」と判断して続けて撃つ）。CraftBukkit は `handleUseItem` で「直前の `RIGHT_CLICK_BLOCK` と**同じ位置・同じ手・`ItemStack.isSameItemSameComponents` が真**なら2発目の `PlayerInteractEvent`(`RIGHT_CLICK_AIR`) を発火しない」という抑止を持つ。ところが `XpBottleListener#consumeOneAndGive` は**スタックが1のときだけ手の中身を差し替える**（2本以上なら手には同じガラス瓶が残り新しい瓶は別枠へ入る）ので、**スタック1のときだけこの抑止が外れて2発目が届き**、入れ替わった手を見て逆の操作を実行する＝報告どおり操作が丸ごと巻き戻る。⚠️ **「スタック1のときだけ」という条件はここから来ている** ── 二重登録でもオフハンド発火でもない（`XpBottleListener` の登録は `TrinityForge.java:1250` の1箇所のみ、`getHand()!=HAND` は既に弾いている）。→ 同tickに既に格納/取出を**実行した**プレイヤーなら2発目を無視する（空振り＝水汲み・経験値0・未解放は記録しないので通常のクリックは取りこぼさない）。**無視しつつイベントはキャンセルする** ── バニラに譲ると充填済みの瓶がそのまま投げられて中身が消えるため。回帰3本（`secondEventOfTheSameClickDoesNotUndoTheStore` / `…DoesNotRefillTheBottle` / `aLaterClickIsStillHandled`）で、ガードを外すと前2本が実症状そのまま（500へ復元 / 0へ再吸収）で落ちることを実走確認。TF 4660/失敗32/skip2 ── 失敗32は他セッションの未コミット yml 由来で XpBottle 系は 17/0/0 |
| 2026-08-24 | **一括伐採で原木以外が落ちない件の切り分け**（報告「苗木・りんご・きんりんご等がおちません」W-219）。⚠️ **リンゴ/金リンゴ/クリスタルリンゴは既に `394ec97` で直っており、配備が追いついていないだけ** ── `394ec97`(08-24 02:44:47) が `TreeFellingListener#rollChainDropTables` を足したのに対し、配備先3台の `TrinityForge-0.1.0-SNAPSHOT-all.jar` は **08-24 02:26:40**（18分古い）。実際に zip を開いて `WoodcuttingGimmickConfig.class` に `treeFellChainDropRollsMax` が**無い**ことを確認済み。配備先の `stats/woodcutting-gimmick.yml`(01:42) にも `chain-drop-rolls-max` が無い（既定8で動くので config 側は必須ではない）。→ §3.5 の jar 配備で解消する。⚠️ **苗木はこれとは別系統でまだ説明がついていない** ── 苗木/棒/バニラのリンゴは TF のドロップテーブルではなく**バニラの葉のルートテーブル**で、連鎖分の葉も `ChainBreakSupport#breakOnce` が `getDrops(tool, player)` を引いて `dropItemNaturally` で撒いている（`breakLeafBatch` が渡す `false` は `dropItems` ではなく `consumeDurability`）。`removed-vanilla-items` も `ANY:MENDING` だけで苗木は消していない。**最有力は「そもそも葉が1枚も壊れていない」**＝`LeafDecayPlanner` の既知の仕様（幹を `max-extra-logs` まで伐り切れない木では、残った幹が距離6以内で樹冠を支え続けるので「バニラなら崩壊する葉」が0枚になる）。**切り分けの決め手はアクションバー**: 「一括伐採 xN (葉 xM)」と出ていれば葉は壊れており別原因、「一括伐採 xN」だけなら葉が0枚＝この仕様。⚠️ **`stats/woodcutting-gimmick.yml` はワーキングツリーで他セッション/エディタが編集中**（tier4 を削って3段へ、金リンゴ 1%→0.5%、クリスタル 0.1%→0.05%、`enchant`(エンチャント金リンゴ 0.01%) を追加）。`WoodcuttingGimmickConfigTest` の1件はこれ由来なので触っていない |
| 2026-08-24 | **スキルツリーで「チェーンの続きがレベル帯を1つ飛ばして置かれる」配置崩れを潰した**（報告「総合で足元強化のあたりのノードが変なつながり方している。設定値ミスってる？」W-218）。⚠️ **答えはノー: ノードの座標は yml に無い**（`SkillTreeLayout` がレベル・親・役割から機械的に決める＝jar だけで直る）。枝ノードは「親のちょうど1段上」に置かれるが、総合では lv40 の行の内側2レーンを先に置かれる**持久 I / 回収 I**（活力の充実の枝）が取るため足元強化 II が入れず、旧探索順（2026-07-30 改修の「横へ大きく迂回する前に1段上へ逃げる」）で **lv60 の行**へ落ち、III が玉突きで **lv80 の行**へ。結果、足元強化 I→II のコネクタが**持久と回収の間の通路を縦に3マス走ってから横へ折れる**＝報告の「変なつながり方」。⚠️ **単に順序を入れ替えるだけでは直せない** ── 横へ探すと通常枝が排他路線側の半平面へ滑り込むので深さ優先にしていた、というのが旧実装の事情。→ **①空きセル探索を行優先**（まず同じ行を外側へ、行が尽きてから1段上へ）**②その探索に半平面ガード**（排他路線を持つツリーでは通常枝は主軸をまたがない）の2つセットで修正。**ユーザー要求で事前シミュレーション**: 旧挙動を再現する `SkillTreeLayout(tree, rowFirstSearch=false)` を残し、出荷16ツリーの前後を同一実行で比較する `SkillTreeLayoutRowFirstComparisonTest` を追加（指標＝帯ずれ／接触／迂回／主軸列侵入／線の重なり）。結果は **帯ずれ合計 7→1・接触/迂回/主軸列侵入は全16ツリーで増減ゼロ・線の重なり合計 159→156**。動いたのは4ツリー（総合1→0／魔法2→0・高さ13→11／魔法鍛冶1→0／付呪3→1・高さ11→9・幅13→15）で、総合で座標が変わったノードは**5つだけ**（足元強化II (6,2)→(8,4)＝lv40の行へ／足元強化III (6,0)→(8,2)／天恵II (8,4)→(10,4)／回収II・III (8,2)(8,0)→(6,2)(6,0)＝回収Iの真上へ整列）。⚠️ **唯一のトレードオフ**: 総合の「線の重なり」が 10→11 に1つ増える（内側チェーンが外側チェーンのレーンを飛び越すときは必ずどこかで1マス通路を共有するので原理的に不可避）。帯ずれの方が実機で読みにくいのでそちらを優先し、テストは**ツリー単位で帯ずれ/接触/迂回/主軸列侵入の悪化を禁止・線の重なりは合計で悪化しないこと**を縛る。⚠️ **別件で発見**: `skilltree/ars_magic.yml` の `B-1-1`（氷風Ⅰ）が `role: intermediate` で主軸列の B(2,6)・C(2,4) の間 (2,5) に挟まり上下ともコネクタの隙間ゼロ（`AllSkillTreesLayoutIntegrityTest`/`AllSkillTreesProgressionTest` が赤い原因。**他セッションの未コミット編集**なので未着手。`role: branch` が正と思われる）。TF 4657/失敗32/skip2 ── 失敗32は棚卸し時点と同一の他セッション未コミット yml 由来 |
| 2026-08-24 | **統合版の「ネザー後に空がネザーのまま」を参加時の自動修復で塞いだ**（GeyserAddon `2a5a335`、実機検証待ち）。⚠️ **真因はクライアント側**: ポータルの霧遷移中に Velocity の `/server`（またはリログ）が挟まると、移動先が次元変更パケットなしでチャンクだけ読み直すため、統合版クライアントが現世でもネザーの空を描き続ける（GeyserMC #3005 系・上流未修正。実機再現は「資源鯖のポータル内で /server main」）。`/server` はプロキシ組み込みでバックエンド側から遮れない＝**予防は不可能、移動先での修復のみ**。→ 退出時に「環境・ポータル内か」を共有 extension フォルダ `dimension-handoff/<uuid>.json` へ記録し、参加時にフラグが新鮮(60s)でポータル内/環境不一致、または現にポータルブロック内に立っている統合版プレイヤーだけ、別環境ワールド（エンド優先）へ往復テレポートして次元を再構築させる。往復中の切断は戻り先を `.return.json` に永続化して次回参加で復帰（置き去り防止）。セッション中に壊れた場合の手動脱出路 **`/fixsky`** を追加。skin-fix-only の裏鯖でも有効（スキン修復と同型の「全バックエンド常駐」系）。config `general.bedrockNetherSkyFixEnabled`（既定on・Boxed）。判定と台帳は Bukkit 非依存に切り出しテスト15本、全体 389 全緑 |
| 2026-08-24 | **ACTIVE_RECORD を全面棚卸し**（ユーザー指示「完了済みは削除かアーカイブ化し、判断が要る残タスクを洗い出す」）。旧 §1 スナップショット・旧 §2（J-11〜J-19 全決着）・旧 §3・バッチ節（W-28〜W-215 ほか約 60 節）・解決済み K 行 7 件（K-7/K-9/K-10/K-12/K-20/K-24/K-37）・§7 の 2026-08-22 以前をアーカイブ §B の「2026-08-24 移設分」節へ**原文のまま**移し、現役分を §2（判断待ち D-1〜D-18／情報待ち I-1〜I-6）・§3（実装タスク）・§3.5（配備・運用待ち）へ再編した。**項目の中身・状態の判断は変えていない**（移設と再分類のみ）。現役として残した根拠は各行の背景欄に記載 |
| 2026-08-24 | **EMダンジョンで「中ボスがボスより硬い/柔らかい」役割の逆転を潰した**（報告「EMダンジョン設定で中ボスの方がボスより火力が高くHPが雑魚より低いらしい。おそらく以前のバランス調整が反映されていない？」）。⚠️ **HPと攻撃で EM 側の値の扱いが逆**という非対称が真因。**HP: `EliteEntity#setMaxHealth` が `maxHealth = tfMaxHealth * healthMultiplier`** ── TFが計算し終えたHPに EM の `healthMultiplier` が**後段で掛かる**ので、W-181 が付けた役割差（ボス/中ボスに ×1.236）が、EM 側の 0.0001〜120（ダンジョン内で最大991倍の開き）に呑まれる。**攻撃: EM の `damageMultiplier` は TF が捨てている** ── `TrinityForgeSpawnListener#stamp` が `MOB_ATTACK_POWER` を刻むと TF の `CombatListener`(HIGH) が EM の NORMAL 優先度の計算結果を丸ごと作り直す（`SymmetricCombatService#physicalFinalDamageFromMobResult` は `itemAttackPower != 0` なら vanillaBaseDamage を使わない）。配備先の lua 68 本にも `damage_multiplier` の参照はゼロ。**＝設定ファイル上の火力の逆転は実ゲームに出ない**（実際に効く TF 側 `attack-power-multiplier` は ボス1.103 > 中ボス1.07 > 雑魚1.0 で逆転ゼロ。ただし差が小さく大聖堂で1発 ボス15.1/中ボス14.6/雑魚13.7 なので「中ボスも同じくらい痛い」体感にはなる）。⚠️ **「以前の調整が反映されていない」は半分だけ正しい** ── 2026-08-14 の難易度再設計は EM 倍率を**織り込んでいる**が、対象は「ダンジョン全体の必要総HP」の単調性（7.30M→69.37M）だけで、**ダンジョン内の役割ごとの序列は対象外**だった。⚠️ **役割の判定に `mob-overrides.yml` のコメント（`# 雑魚` / `# ボス第N段階`）を使ってはいけない** ── 自動生成で誤りがあり（`昇降機の管理者(最終段階)` や `シャルルマーニュ(第4段階)` が雑魚扱い）、これで数えると偽の逆転が16件出る。**正は EM の `bossType:`（NORMAL/MINIBOSS/BOSS/REINFORCEMENT/EVENT、未記載は NORMAL）**。ただし `BossType` は `CustomBossesConfigFields` が読むだけで**どこからも参照されていない純粋なメタデータ**。→ `bossType` で数え直すと**実体は3体だけ**（残り13件はこの3体を基準にしたことによる派生）: `em_id_the_deep_mines_slow_giver_dummy`(EM 99.1 → 実効131.5倍・ボスの2.2倍)／`dark_cathedral_tier_75_reinforcement`(EM 50.25 → 実効69.1倍・ボスの1.36倍。挑戦Lv34 で増援 110,193 HP vs ボス 81,312 HP)／`halloween_event_boss_mount`(EM 50 → 実効80.2倍・乗り手のボスの1.25倍)。**EliteMobs の custombosses はリポジトリに無く配備先にしか無い**（かつ `D:/game` はエージェント権限で書けない）ので、修正は **TF の `mob-overrides.yml` per-mob `max-health-multiplier` で割り戻す**形にした。上限は**「ボス最大の25%」と「最弱ミニボスの60%」の小さい方**（非ボス）／**ボス最大の60%**（騎乗体のミニボス）── ⚠️ **ボスだけ見て25%にすると今度は中ボスを追い越す**ので2条件セットで見る。0.0241 / 0.0885 / 0.58 を投入し、逆転が (1)3体→0体・(2)13体→0体 になることを `tmp/em-bosstype-inversions.js` で実走確認。大聖堂 Lv34 の序列は ボス81,312 > 中ボス16,262 > 増援9,752 > 雑魚2,193。**config のみの変更**（jar は無変更）。TF 4621/失敗32/skip2 ── 失敗32は全て `gates.yml`/`catalog.yml`/`mob-types.yml` など**他セッションの未コミット yml 由来**で、mob-overrides の守り（`MobOverridesConfigTest`/`MobOverridesMultiplierTest`/`ConfigManagerMobOverrideResolutionTest`/`ShippedBossStrengthDriftTest`/`ShippedMobImportBreakpointTest`）は全て緑。 |
| 2026-08-24 | **創作タブの「糸」スレッドと旧テクスチャの真因を潰した**（GeyserAddon `7d8f7a7`）。⚠️ **`custom_items.json` は「最初の登録が永遠に勝つ」台帳で、一度壊れた登録が二度と治らなかった**。壊れ方は2系統: ①パックが読めない瞬間（02:30 起動の展開消し合い等）に登録が走ると表示名が素材のバニラ名「糸」で固定される（`custom_string_300002` 等 **43 件**。名前が「ある」せいで以後の昇格経路が全部辞退）②CMD を保ったまま基材が変わった品は旧基材のエントリが幽霊で残る（**スレッドは防具装飾テンプレート基材の旧世代 60 件**が創作タブに旧アイコンのまま出続けた＝「旧テクスチャ」の正体。さらに現行 (string,cmd) 側は **約31件が未登録**で実物も糸表示）。→ `PackRegistryReconciler` 新設: 起動時のパック事前登録を **add / heal / evict** の照合へ（heal は自動命名×プレースホルダ名のときだけ・運営の手付けは不触、evict は「パックに無い基材×CMDと表示名が現行品と両方一致」の幽霊だけ）。スキャナ側も素材名フォールバックを「実名あり」と誤認しないよう分離し、実物観測で治るようにした。回帰11本。**配備後の初回起動で台帳が自動修復される**。切り分けの決め手: 台帳の `thread_speed` が `raiser_armor_trim_smithing_template` 基材で登録されていたこと（catalog は一貫して STRING）|
| 2026-08-24 | **統合版レシピブック連打の増殖の真因を塞いだ**（GeyserAddon `0c4feb3`、実機再現待ち）。⚠️ **オフハンド代行（`OffhandSwapListener`）の「キャンセル→イベント時スナップショットを翌tickに書き戻す」方式が複製機だった** —— レシピブックのクラフトは1tick内のクリック列に翻訳され、列中の1クリックがオフハンド枠に当たると、残りの列が同tick内にその素材をグリッドへ移して消費した後、翌tickの書き戻しが**消費済みの素材をオフハンドへ鋳造する**（「一部欠損して配置され、欠けた分がオフハンドに入る」という報告どおり）。→ スナップショットを前提条件化: 遅延実行時にカーソル/オフハンドが観測時と不一致なら**何も書かず中止**（クリックはキャンセル済みなので中止は常に安全、`updateInventory` で統合版の予測だけ矯正）。⚠️ **paper モジュールでは `ItemStack` の構築自体がサーバを要求し単体テスト不能** → 実証は実機での連打再現で行う。ついでに **ブロック3Dアイコンを 64→128px に**（`f79869a`。統合版バニラは実3D描画なので、GUIスケール2〜3のスロット96〜160pxで焼きPNGが拡大されて甘く見えた）|
| 2026-08-24 | **Java リソパが1世代未配布だったのを `pack-20260824121454` として配布**（sha1 `4e9748ec…`）。08-22 リリース以降の dist 再ビルド分（トレジャー専用スレッド10種・ソース階梯細分化・圧縮素材の精錬・無限ソース核の採番）が GitHub release に上がっておらず、**Java も統合版も新アイテムを旧テクスチャ/無テクスチャで見ていた**（「旧テクスチャ」のもう1本の原因）。⚠️ ビルド時に整合性チェックが落ちた: **圧縮アメジスト(5447)の items json 配線がワークツリーで HEAD から巻き戻されていた**（HEAD の内容へ復元して解決）。**残り3段はユーザー実行**: ①`set-resource-pack.ps1 -Url <新URL> -Sha1 4e9748ecb84bf057853f5c9efab447c5d91a9de3` ②`deploy-full.cmd`（geyserExtra jar 3,974,639 バイト＝展開消し合い修正+台帳修復+鮮度ガード+128pxアイコンを含む）③統合版は次回接続でパック再取得 |
| 2026-08-24 | **統合版がらみの配備を `ops\launch\deploy-full.cmd` 1 本にまとめた**（新規 `ops/launch/deploy-full.cmd` + `ops/scripts/deploy-full-plan.ps1`）。⚠️ **`deploy.cmd` は GeyserExtra を配らない** —— 別リポジトリなので対象外。今まで手で 4 コマンド打っており、**順序を間違えると症状が「レシピが全部消える」という無関係な形で出る**。→ 計画表示 → `DEPLOY` 入力 → **ビルド → 停止 → 配備 → 起動**を通しで行う 1 本へ。設計上の要点3つ: ①**ビルドを停止より前に置く** —— `deploy.cmd` は稼働チェックの後にビルドするので**コンパイルエラーの代償がネットワーク停止**になる。先に建てれば失敗しても無停止で終わる。②**GeyserExtra → TrinityForge の順に固定** —— TF が書く `bedrock-recipes.json` は形式2で、旧 GeyserExtra の collector は 1 しか受けず**表を丸ごと reject する**（逆順の skew は 1 も 2 も受理＝安全側）。③**既存スクリプトを合成するだけで `deploy.cmd`(500行)には触らない**（実サーバ相手にしか試せないものを書き換えない）。踏んだ罠: ⚠️ **`.cmd` の素の `shift` は `%0` も繰り上げる**ので、オプションを1個食った時点で `%~dp0` が「このスクリプトの場所」でなくなり兄弟スクリプト呼び出しが全部壊れる（`shift /1` ＋ 先に `set "SELF=%~dp0"`）。⚠️ **Windows PowerShell 5.1 の `Sort-Object` は安定ソートではない**（PS6 まで `-Stable` が無い）ので、計画を並べ替えると肝心の「GeyserExtra が先」が表示上だけ崩れる → 組み立て順のまま出す。⚠️ **配備先の TF jar 名は `TrinityForge-0.1.0-SNAPSHOT-all.jar` で成果物名と違う** —— 成果物名で置くと jar が2つ並び `Ambiguous plugin name` で Paper が起動しない → ワイルドカードで「今入っている名前」を拾う。⚠️ **リポジトリ名が末尾ギリシャ文字αで `.cmd` は ASCII 限定**なので、名前を書かず `geyserExtra*` のパターンで兄弟ディレクトリを探す。**配らないもの**: config(yml) は `deploy-config-head.cmd`、フォークは `deploy.cmd`。動作確認: `--dry-run` 実走（読み取りのみ・計画正常）／バイト検査で CRLF 190・裸 LF 0・非 ASCII 0・BOM 無し／終了コード `--help`=0 / 不明オプション=1 / `--dry-run`=0。`check-ops-scripts.cmd` は 46 PASS / 1 FAIL だが、**この 2 ファイルを退避して再実行しても同じ 1 FAIL**（preflight の既存項目）と確認済み。⚠️ **`ops\launch\deploy-launch.cmd` を走らせないと配備先の `launch\` には届かない** |
| 2026-08-24 | **統合版アイコンを実機へ配備し、起動ログに出た `NoSuchFileException` 537 件の真因を潰した**（GeyserAddon `cc754c4`）。配備自体は成功（配信中パック 1,588,859 バイトを直接検査し、**焼いたアイコン 45 種がバイト一致で載っていること**を確認。`decorated_pot` ×7＝ソースジャー、`lectern` `iron_chain` `wither_rose` も）。⚠️ **`JavaPackResolver#extractZip` は resolve のたびに `deleteRecursive` してから展開し直す。** 1 本なら無害だが、**パック再生成はスケジューラスレッドで走り、2 本が同じ `java-pack-extracted-server` を指す** —— 片方がテクスチャを読んでいる最中にもう片方がディレクトリごと消す。読めなかった分は**そのまま統合版のバニラ表示に落ちる**（「一部アイテムが元の素材のまま」の一因）。**切り分けの決め手はログの警告件数が 344 → 193 → 0 と減っていくこと**（展開が進行中に読んでいる形。前日の起動は警告 0 で、パックが大きくなって生成が重なるようになった結果あらわれた）。→ ZIP のサイズ＋更新時刻を展開先の `.source-stamp` に記録して一致時は展開を丸ごと省き、初回と ZIP 変更時は**展開先ごとの静的ロック**で直列化（racing する resolver は別インスタンスなのでインスタンスフィールドでは守れない）。stamp は展開の最後に書くので途中で落ちれば次回やり直す。⚠️ **回帰テストは 1 エントリの ZIP では通ってしまう**（展開が速すぎて衝突しない）。300 エントリで窓を広げて、stamp を無効化すると 2 件落ちることを実走確認。**副産物**: パック版の消費が 1 起動あたり 3 → 1 に減る（上限 32767 に対し現在 32268）。362 tests, 0 failures, 0 skipped。**この修正は未配備**（実害は起動直後の十数秒に接続した統合版プレイヤーだけなので、次の再起動でよい） |
| 2026-08-24 | **「まだ Java と全然違う」の真因2つを直し、統合版アイコンを Java の実描画と一致させた**（GeyserAddon `7a3459e`）。①**UV を `texture_size` で縮尺したのが逆**（前行の訂正参照。スカルが白/黒立方体になっていた正体）。②**entity モデルには display が無く、ブロック既定の `[30,225,0]`＝背面・斜め下で描いていた**。Java はこれらを **item モデル側の display**（全員 `[30,45,0]` 正面向き。壺は scale 0.60 + gui_light front、コンジット scale 1.0、スカルは translation [0,3,0]）で描く。⚠️ **スカルの display は `items/*.json` の special ノードの `base`（`item/template_skull`）経由でしか取れない**。同族も全部拾った: シャルカー16色+無色（⚠️ **`dyed_shulker_box` テンプレートは布が tint 前提**なので緑に染まる。無地モデル+色別シート差し替えで回避）・銅チェスト4酸化+ワックス（通常チェストの形+銅シート）・銅ゴーレム像8種。さらに**立方体近似171枚の正体は「Java では平面スプライトのアイテム」**（苗木・看板・レール・松明・花・扉・板ガラス…）だったので、item モデルが `layer0` に解決されたらそのスプライトを焼く一般則を追加し手書き12件を置換。ついでの罠3つ: ⚠️ **Mojang 自身の `block/heavy_core` が `"texture": "all"` と `#` 無しで変数参照**（本家は寛容に解決する。裸名も変数として解決するようにした）／⚠️ **同一 Bedrock 名に複数 Java ID が結ばれ、アイテム無しの状態ブロック（`lava_cauldron`・`oak_wall_hanging_sign`）が反復順で勝つ**（アイテム定義を持つ ID を常に優先する2段階へ）／⚠️ **対応表に素の `cauldron`／`oak_hanging_sign` 自体が居ない**（Bedrock 名と同名の Java アイテムが在れば候補に足す）。焼き上がり 959 枚: **Java 実モデル 781 / 平面スプライト 148 / 立方体近似 30**（前: 751/3/203。残る30は Java にアイテムが存在しない壁看板・旗・ベッド・作物・水/炎/ポータル類のみ＝これ以上の正解が存在しない）。359 tests, 0 failures, 0 skipped。追補（`4d71613`）: ⚠️ **銅ゴーレム像だけ逆立ちして焼かれていた** —— Mojang の `item/template_copper_golem_statue` は **gui rotation `[30,45,180]` と Z 軸 180° ロール付き**（クライアント内部の像ジオメトリが逆さに作られているため display 側で毎回反転している）。正立の同梱モデルにこれを適用すると逆立ちし、translation も向こうのジオメトリ前提で流用不可。像だけ display を合成エントリで上書き（ロール抜き `[30,45,0]`・scale 0.55・translation は同梱モデル実寸 y 0..24 の中心合わせから −1.9 を導出）。**配備は GeyserExtra の paper jar のみ**（2,239,471 バイト） |
| 2026-08-24 | **統合版アイコンで「立方体のまま」だった飾り壺・コンジット・チェスト・頭を実物の形にした**（GeyserAddon `4f1821b`）。⚠️ **Minecraft がコードで描くブロックは Java にも Bedrock にも形が無い** —— `models/item/decorated_pot.json` は particle テクスチャだけ、Bedrock 側の `entity/decorated_pot.entity.json` も**テクスチャ 24 件で geometry セクションが無い**（Mojang の bedrock-samples に壺・チェスト・ベッド・旗の geometry は 1 本も無い）。**既製の「3D インベントリ用テクスチャ集」も 1.21.11 には無い** —— `undrfined/mc-icons` は 1.12 時代の**数値ブロックID**（`103_Melon Block.png`）で飾り壺が存在しない、Minecraft Wiki の `Category:3D inventory icons` は現役 1 件・残り 1100 件が「Outdated」、`voxel-icon-generator` は**生成器**でこちらと同じ仕事をするもの。→ **BlueMap (MIT) が同じ問題のために Blockbench で起こした entity モデル 49 本を同梱**し、Java アセットが 404 のときだけ同じ相対パスの同梱物を読む（将来 Mojang が本物を出したら遠隔側が勝つ）。~~⚠️ これらは UV が `texture_size` 空間（壺 32・頭 64）でバニラの 0..16 ではない~~（**2026-08-24 訂正: 逆**。UV はバニラ同様 0..16 空間で `texture_size` は Blockbench のメタ情報。縮尺を掛けるとスカルが無地領域を読んで白/黒の立方体になる。壺のテクスチャがほぼ無地なので目視検証をすり抜けた → GeyserAddon `7a3459e`）。⚠️ **チェストだけモデル名がブロック名と違う**（`entity/chest/normal`）。⚠️ **同梱が classpath から外れると全部が黙って立方体へ戻る**。既存の「半分未満なら失敗」ガードでは全く引っかからないので、**5 ブロックが実モデル由来であること**を生成時に検査し、**その一覧が 1 件も一致しない場合も失敗**にした（一致ゼロの許可リストは検査そのものを無効化するため）。焼き上がり 957 枚のうち Java 実モデル由来 **738 → 751**、立方体近似 **216 → 203**。core 33 / paper 295 / extension 31 = 359 tests, 0 failures, 0 skipped。**配備は GeyserExtra の paper jar のみ**（2,333,955 バイト）|
| 2026-08-24 | **統合版クラフト修正を実機で確認して出た 3 件を処理した**（TF `5831b88` / GeyserAddon `f432c19`）。①**「クラフトはできたのに『材料にできません』の通知が出る」** → 判定ではなく**通知の出しどき**の問題。⚠️ **`PrepareItemCraftEvent` はマスを1つ触るたびに飛ぶ**ので、カタログ品を並べている途中の盤面は当然どのレシピにも一致せず `getRecipe()` が null になる。そこで名指ししていた＝**まだ何も奪っていないのに止めたと言っていた**。⚠️ **統合版では必ず起きる** —— Geyser はクラフト要求を**マスへの1つずつの配置**へ翻訳するので、成立するレシピでも途中で不一致の盤面を必ず通る。レシピが届くようになった瞬間に表面化した。→ 選択レシピが非 null のときだけ通知（W-87 の守りたかった場面は残る）。回帰2本、無条件に戻すと落ちることを実走確認。②**焼いたアイコンが平面で低画質だった** → ⚠️ **「立方体でないブロックは平面」という線引きが乱暴すぎた**。Java はアイテムモデルの親で描き分ける（`block/*`＝gui 変換で 3D、`item/generated`＝平面スプライト）ので、壺・書見台・金床・焚き火・エンドロッドは**立方体近似のほうが実物に近い**。平面のままは鎖・ろうそく・ウィザーローズ・大釜など 5 件へ。⚠️ **32px では 16px テクスチャの 1 テクセルが投影の対角線上で出力 2px しか持てず縁が階段状になる** → 64px（1 テクセル 4px で面の境界が整数ピクセルに乗る）。paper jar 1.5MB→2.6MB。③**「統合版に古いデータが残る？」** → **配備中パックは現レジストリの CMD 持ち 710 件を 100% 網羅**（テクスチャキーも lang も欠けゼロ、実測）。パックのパッチ版は**内容変化ごとの単調増加カウンタ**（現在 1.0.32262 / 上限 32767 で version[1] へ繰り上がる設計）なので、内容が変われば必ず番号が変わる＝**取り違えは構造的には起きない**。⚠️ **起動ログの `Left 500 PDC-only mapping(s) as plain vanilla items` が「元の素材名・テクスチャなし」の正体**（アイコンもアタッチャブルも無い品を独自定義にすると、バニラ id を要求する Bedrock レシピを壊すので意図的に素通し）。残りは端末側キャッシュ。**TF jar と paper jar の両方が未配備** |
| 2026-08-23 | **統合版でカスタム素材のクラフトが通らない件の真因を潰した**（GeyserAddon `83ecd27` / `0843e5e` / `d855674` / `9869346`、**TF 本体は無変更**）。⚠️ **「アイテムが壊れている」ではなく「レシピがクライアントに届いていない」** —— 統合版は*どのレシピを作るか*をクライアントが決めるので、届いていないレシピは存在しないのと同じ。プロキシログの `sent 157 corrected recipes but dropped 203` が唯一の物証。連鎖は **ブロック基材 → 平面アイコンが無い → `CustomItemsHandler#shouldUseVanillaBaseFallback` が独自定義を登録しない → Bedrock 識別子が無い → `BedrockRecipeInjector` が名指せずレシピを丸ごと落とす**。2026-07-22 に「アイコンを解決できないブロック系はバニラに委ねる」と決めた当時、レシピ注入はまだ無かったので代償がクールダウン分類だけだと見積もられていた。⚠️ **`geysermc:block_placer` では直せない** —— `geysermc:` 名前空間の成分は `ItemDataComponent.vanilla()` が全部 false で、バニラ由来の定義に付けると例外（javap で確認）。→ **Java が gui 変換で描いているのと同じ等角投影の立方体をビルド時に焼いて jar へ入れる**（`IsometricBlockRenderer` / `BedrockBlockIconGenerator` / `BakedBlockIcons`、948 枚）。面の陰影は Minecraft と同じ 上1.0/左0.8/右0.6。⚠️ **立方体でないブロック（壺・書見台等 13 件）はスキップせず平面で焼く** —— 無アイコンは見た目が劣るのではなく**クラフトごと不可能になる**。⚠️ **bedrock-samples の `blocks.json` は Geyser と違う古い名前でブロックを持つ**（`iron_chain`→`chain` / `grass_block`→`grass` / `sea_lantern`→`seaLantern`）ので別名表が要る。⚠️ **焼き損ねても以前はビルドが成功していた** —— 取得元がネットワークなので clean 後の無ネット環境がアイコン0枚の jar を吐き、テストも赤くならない → 404 以外の失敗と「対象の半分未満」でビルドを落とす。**別件で `BedrockEnchantmentHandler` がクリエイティブの統合版プレイヤーへ表示名を注入していたのを止めた**（`SET_CREATIVE_SLOT` の書き戻しで `custom_name` がアイテムへ焼き付き、**スタックしなくなる**。lore と違い注入名にはマーカーが無く除去経路も無い。既に焼き付いた分は直らない）。配備データでの試算で**落ちるレシピ 205→3**、残り 3 本は配備中パックがレジストリより古いだけでコードの問題ではない。core 26 / paper 295 / extension 31 = 352 tests, 0 failures, 0 skipped。**paper jar と extension jar の両方を再配備が必要**（未配備） |
| 2026-08-23 | **プレステージの誤爆を確認モーダルで塞ぎ、プレステージ枠へ赤字警告を注入した**（実サーバ報告 W-188）。⚠️ **2026-08-21 の「2クリック確認」は方式そのものが誤爆を防げていなかった** —— 確認と実行を**同じマスに重ねて**いたので、トーテムを押した 1 tick(50ms)後に同じスロットが確定ボタンへ変わり、**ダブルクリックの2打目がそこへそのまま入る**（統合版やタッチ操作では素の操作として出る）。プレイヤーから見れば「1回押しただけでプレステージされた」。⚠️ **前回の回帰テストがこれを守っていた** —— `secondClickConfirmsThePrestige` が「同じスロットをもう一度押すと確定する」を正しい挙動として固定していた。→ `Mode.PRESTIGE_CONFIRM` を新設し、赤字警告＋「はい」「いいえ」だけのモーダルへ分離。⚠️ **画面を分けるだけでは足りない**ので `prestigeYesSlot(originSlot)` で**直前に押したスロットには「はい」を置かない**（衝突するときは「はい」と「いいえ」を入れ替えるので、2打目は必ず取り消しになる）。確認画面は `lastView` へ覚えさせない（覚えると開き直しただけで確定ボタンのある画面が出る）。赤字警告は描画時注入で**確認モーダル/ツリー本体/パーク一覧の3画面が同じ `prestigeWarningLore` を通る**（2箇所で書き分けると片方だけ「0に戻る」が抜けて嘘になる）。⚠️ **POWER だけレベルの戻り方が違う**ので文言は `prestigeLevelNotice` の分岐を経由する。回帰6本、**衝突回避を外すと2本落ちる**ことを実走確認。**TF の jar のみ**（config・リソパは無変更） |
| 2026-08-23 | **構造物ルートのティア専属化・試練の間の空チェスト・`reset-resource.cmd`・トレジャースレッド10種**（実サーバ報告 第30陣 W-187a〜e）。⚠️ **「固有のものが少ない」は体感ではなく実測どおり** —— 品質系スレッドは1種あたり **9/10 ティア**、カスタムモブ素材は **10/10 ティア**に出ていた（＝どこを漁っても同じ）。看板ティア+隣接1/3 の2ティアまで、素材は浅/中/深の3帯へ、カギは全種ちょうど2ティアへ絞り、新スレッド10種は**1種＝1ティア専属**にした。⚠️ **`loot-tables.yml` は生成物**なので真源は `tmp/worldgen/gen_loot_yml.py`。⚠️ **試練の間が空だったのは2段の原因が重なっていた** —— ①`pools` にトライアルチャンバーの表が1本も無かった（2026-08-16 に経済理由で意図的に除外）②**足しても当たらない**。ヴォールトと試練のスポナーは戦利品をブロックが直接排出するので **`LootGenerateEvent` を発火しない**（PaperMC #11680 は「対応しない」でクローズ）。唯一の入口は Paper 1.21.10 の `BlockDispenseLootEvent` で、ArsPaper の `LootTableListener` へ2つ目の入口として実装（本体は `applyPools` に集約。**片方だけ直すと宝物庫だけ空に戻る**）。⚠️ **ヴォールト/スポナーの表は `structures.json` に載らない**（piece ではなくブロックが持つ）ので明示回収し 100 本を発見。⚠️ **1.21.8 生成のワールドを 1.21.10+ へ上げるとヴォールトが恒久的に不活性になる既知バグ**（PaperMC #13521、未修正）があり、これはプラグインでは直せない。⚠️ **`reset-resource.cmd` は引数なしだと必ず dry run で終わっていた**（本実行の経路が無かった）→「dry run → `RESET` と打たせる → 本実行」の2段へ。シードは `level-seed=` が空なので本実行すれば毎回別地形。**editor の構造物ルート抽選はデータパックではない**のでワールドリセット無しでも次のチェストから効く。⚠️ **新スレッドの使える軸はちょうど10本しか残っていなかった** —— ①ATTACK+FLAT は帯非依存性のガードで禁止 ②**ATTRIBUTE は「装着しても効かない」と「手に持つだけで効く」が同時に起きる**（addon が属性経路に合流しない／`AttributeProjection` がバニラ属性として実際に付ける）③`flat-defense` は typed キーがあると無視される別名。一度 ATTRIBUTE で組んでテスト2本が実際に落ちた。結果、**汎用近接は3本しか作れず**残りは遠距離4・防御3（依頼は「やや強めの汎用戦闘系」だが、既存75種が汎用近接の軸を全部押さえているため。W-187e の「相互互換を発生させるな」を守るとこうなる）。**次に増やすなら既存の整理か新ステの新設が先**。TF フル 4582/44失敗/2スキップ（着手前45から自分の修正1本ぶん減、残りは他セッションの WIP と同一集合）、ArsPaper fork 516/0失敗/0スキップ。**TF の jar は不要**。必要なのは config 配備・**ArsPaper jar**・**リソパ再配布**・`deploy-launch.cmd` の4つ |
